# Progress — سجل التقدم والملاحظات

هاد الملف للتحديث المستمر. الخريطة الثابتة (`roadmap.md`) ما تتعدّل — أي تعديل، فكرة جديدة، أو ملاحظة أثناء البناء تُكتب هون، وبعدها نرجع للخريطة نشوف وين وصلنا وشو الخطوة الجاية.

---

## طريقة الاستخدام
- كل ما نخلص prompt/خطوة: نضيف سطر هون (شو خلص، تاريخ، أي ملاحظة أو انحراف عن الخطة)
- كل ما تطلع فكرة جديدة أثناء البناء: تُكتب هون كـ"ملاحظة معلّقة" — ما تُدمج بالخريطة الأساسية إلا بعد ما يشتغل الديمو بالكامل
- قبل كل جلسة عمل جديدة: نراجع آخر سطر هون + نرجع لـ`roadmap.md` نحدد الخطوة الجاية

---

## سجل التقدّم

- **2026-09-15 — طبقة المنصة المشتركة (قسم 8):** Next.js 16 (App Router + TypeScript + Tailwind v4) + Neon Auth (Managed Better Auth، عبر `@neondatabase/auth` و`@neondatabase/auth-ui`) + جدول `game_state` (`user_id UUID` مرتبط FK بـ`neon_auth."user"`, `data JSONB`). صفحات `/auth/sign-up` و`/auth/sign-in` (مكوّن AuthView جاهز)، `/dashboard` محمي (middleware + فحص جلسة سيرفر)، `game_state` بينعمل له upsert تلقائي أول ما المستخدم يوصل الداشبورد. **تحقّقت من الأربعة بنود بقبول الخطوة فعلياً ضد قاعدة بيانات Neon الحقيقية** (مو تخمين): تسجيل حساب حقيقي أنشأ صف بـ`neon_auth.user`، صف `game_state` فاضٍ انعمل تلقائياً، كوكي الجلسة `Max-Age=604800` (7 أيام — تدوم بعد إغلاق المتصفح)، و`/dashboard` رجع 307 لصفحة الدخول لما ما في جلسة. بيانات الاختبار انحذفت بعدين. **انحراف عن الخطة:** بدأنا فعلياً بـ`@stackframe/stack` (Stack Auth القديم) بناءً على تسمية "publishable/secret key" يلي طلبها المستخدم، لكن تبيّن إن مشروع Neon الفعلي مبني على نظام Neon Auth الجديد (Managed Better Auth، متغيرات `NEON_AUTH_BASE_URL`/`NEON_AUTH_COOKIE_SECRET`)، فتم التحول الكامل لهيك. ملاحظة أمان: المستخدم حط بالغلط DATABASE_URL حقيقي بملف `.env.local.example` (يلي كان متتبّع بـGit) — انصلح فوراً (القيمة نقلت لـ`.env.local` المستثنى من Git، والـexample رجع قالب فاضي). **تحديث لاحق (نشر Vercel):** لقينا مشروع Vercel مربوط بالمستودع بدون Framework Preset (كان `null` لأنه اتربط أصلاً على كومِت "test" قبل أي كود) — كل الطلبات كانت ترجع 404 من طبقة Vercel نفسها رغم نجاح الـbuild؛ انصلح بإضافة `vercel.json` (`"framework": "nextjs"`). كمان لقينا مشروع Vercel يتيم ثاني (`uae-business-sim` بدون "-luhv") مربوط بنفس المستودع بدون أي تكامل Neon — يستاهل مراجعة/حذف لاحقاً. وبعدها ظهر خطأ `INVALID_ORIGIN` عند التسجيل من الموقع الحي (لأن دومين الإنتاج ما كان مضاف بقائمة الدومينات الموثوقة بلوحة Neon Auth) — انحل بإضافة الدومين من طرف المستخدم بلوحة Neon.

- **2026-09-15 — هيكل عام للمراحل العشرة (القسم 3، بدون تفاصيل):** صفحة `/game` (محمية زي `/dashboard`) بتقرا/تكتب `game_state.data.currentStage` (يبدأ 1 تلقائياً لو مش موجود). كل مرحلة من العشرة (`lib/game-stages.ts`) شاشة بسيطة: عنوان + سطر وصف + زر "التالي" (Server Action `advanceStage`، بيحدّث DB مباشرة). مرحلة 10 (النتيجة) بتعرض "انتهت الجولة" بدل الزر — بدون أي منطق ربح/خسارة بعد. زر "إعادة البدء" (Server Action `restartGame`) موجود بكل الشاشات. `/dashboard` صار فيه زر "ابدأ الجولة" يودّي لـ`/game`. **الأربعة بنود قبول الخطوة تم التحقق منها فعلياً بطلبات HTTP حقيقية + فحص مباشر لقاعدة البيانات** (حساب تجريبي، 9 ضغطات "التالي" متتالية عبر curl لنفس الـServer Action، تسجيل خروج/دخول من جديد بجلسة مختلفة كلياً، وزر إعادة البدء) — مو تخمين. بيانات الاختبار انحذفت بعدها. **تحديث لاحق:** نفس التسلسل اتعمل مرة ثانية محلياً وعلى الموقع الحي بعد الرفع (Vercel عمل auto-deploy من الـpush نفسه، ما احتجنا Redeploy يدوي) — تأكد يعمل أونلاين فعلياً، بما فيه فحص مباشر لصف قاعدة البيانات بعد الضغط على "إعادة البدء".

- **2026-09-15 — مرحلة "دراسة السوق" (مرحلة 2 تحديداً، القسم 3):** أول منطق قرارات وعشوائية حقيقي بالمشروع. كريديت منفصل 30,000 (`data.marketResearchCredit`، مستقل عن أي رأس مال أساسي لاحق) موزّع على 3 قنوات (`lib/market-research.ts`) بتصميم متطابق بصرياً 100% (نفس الـJSX بالضبط لكل قناة عبر `.map()`، بدون أي badge/لون/أيقونة). بعد تأكيد التوزيع (Server Action `confirmMarketResearchAllocation`، بيرفض أي مجموع أكبر من الرصيد سيرفر-سايد كمان مو بس بالواجهة): نتيجة كل قناة تُحسب عشوائياً مرة وحدة (شبكة علاقات 70%، دراسة جدوى 90%، استشاريين 40%) وتُخزّن بـ`data.marketResearchResults` — زر "التالي" ما بيظهر إطلاقاً (حتى لو حاول المستخدم يستدعي الـServer Action مباشرة، محمي سيرفر-سايد بـ`advanceStage` نفسه) قبل ما `confirmed: true` يكون محفوظ. "إعادة البدء" صار يمسح `marketResearchCredit`/`marketResearchResults` بالكامل (مو تصفيرهم لقيمة، حذفهم) حتى تتهيأ المرحلة من جديد بالجولة الجاية. **تحقّقت من كل بند قبول فعلياً:** فحصت الـHTML وتأكدت الكروت التلاتة متطابقة حرفياً (نفس الـclassNames بالضبط)، وباستخدام متصفح حقيقي (Playwright — اكتشفت إن نموذج التأكيد يعتمد على `useActionState` فما بيشتغل بمحاكاة نموذج بدون JavaScript زي باقي الأزرار، فاحتجت متصفح فعلي) جربت تجاوز الرصيد (رفض صحيح + رسالة خطأ + زر التالي ضل مخفي) وتوزيع صحيح (نتائج ظهرت، زر التالي ظهر). فحصت الداتابيز مباشرة: النتائج محفوظة بالضبط متل ما ظهرت بالواجهة، وبعد تسجيل دخول بجلسة جديدة كلياً نفس النتائج طلعت بدون تغيّر بـ`decidedAt` (مو إعادة حساب). إعادة البدء مسحت الحقلين فعلياً من الصف، والدخول للمرحلة من جديد هيّأ كريديت 30,000 نظيف بدون نتائج قديمة. بيانات الاختبار انحذفت. **رُفع بعدين ونُشر لايف، وتأكّد يشتغل أونلاين بنفس الاختبارات.**

- **2026-09-15 — تعديل جوهري على "دراسة السوق" (قبل ما ينرفع الكومِت التالي):** بناءً على توضيح جديد من المستخدم، استبدلنا آلية "توزيع مبلغ حر على 3 قنوات" بالكامل بقائمة **4 خدمات بسعر ثابت** (checkbox، مو نص حر) — **هاد انحراف عن وصف "دراسة السوق" الأصلي بـroadmap.md** (يلي كان بيوصفها كـ"توزيع كريديت حر على 3 قنوات"): دراسة جدوى رسمية (10,000، نتيجة فورية 90/10 زي القديم بالضبط)، استشاريين من إعلانات (2,000، نتيجة فورية 40/60 زي القديم)، وفندق 5 نجوم / فندق نجمتين (5,000 / 2,500) — هدول التنين ما بيرجعوا أي نتيجة فورية إطلاقاً، بس بيخزّنوا علم `data.marketResearchResults.hotelConnection` (مصفوفة، ممكن تحتوي `"5star"` و/أو `"2star"`) لاستخدام مستقبلي بمرحلة "البيع" (لسا ما انبنت). القناة القديمة "شبكة علاقات" (70%) اتحذفت بالكامل. الـServer Action اتسمّى من جديد `confirmMarketResearchPurchase`. **تحقّقت فعلياً بمتصفح حقيقي (Playwright) من كل بند قبول:** الأربعة كروت متطابقة حرفياً بالـclassNames، شراء دراسة الجدوى/الاستشاريين بيطلع نص نتيجة فوراً، شراء أي فندق (لحاله أو الاثنين) ما بيطلع ولا نص نتيجة إطلاقاً (تأكدت ما في تسرب نص "دراسة الجدوى"/"استشاري")، التأكيد بصفر عناصر برضه بيفتح زر "التالي" (قرار فعلي حتى لو "ما راح أشتري شي")، فحصت الداتابيز مباشرة وتأكدت `hotelConnection` مخزّن صح (فندق وحد، أو الاثنين مع بعض `["5star","2star"]`، أو غير موجود لو ما اشترى فندق)، تسجيل دخول بجلسة جديدة طلع نفس السلة المؤكدة بدون إعادة حساب، وإعادة البدء مسحت كل شي وهيّأت المرحلة من جديد. **ملاحظة أمانة صريحة:** بند "تجاوز 30,000 ممنوع فعلياً" — الكود بيرفضه صح (تحقق حسابي سليم، مبني على مجموع الأسعار الفريدة المختارة فقط حتى لو انبعتت قيم مكررة بطلب مصطنع)، **بس رياضياً هالبند مستحيل يصير فعلياً بهالقائمة**: مجموع الأربعة خدمات كلهم مع بعض = 19,500، أقل من الحد 30,000 — فما في أي تركيبة اختيار عادية ولا حتى مصطنعة توصل تتجاوزه بالكود الحالي. الحماية موجودة وصحيحة (جاهزة لو انضافت خدمات أغلى لاحقاً)، بس ما قدرت "أشوفها ترفض" فعلياً بتجربة حية لأنه مستحيل تتفعّل بهالكتالوج تحديداً — بلّغتكم بدل ما أدّعي فحصتها.

- **2026-09-15 — تخفيض سقف ميزانية دراسة السوق من 30,000 لـ15,000 (فوق نفس التعديل غير المرفوع):** تعديل بسيط بناءً على طلب المستخدم — **هاد انحراف تاني عن رقم "30,000" الأصلي بـroadmap.md لهالمرحلة**، عشان مجموع الأربعة خدمات (19,500) يصير فعلياً أكبر من الميزانية، فاللاعب مجبور يضحّي ويختار جزء بس. غيّرت مصدر الحقيقة الوحيد (`MARKET_RESEARCH_TOTAL_CREDIT` بـ`lib/market-research.ts`) — كل الأماكن التانية (التهيئة الأولى، وإعادة البدء) بتقرا منه تلقائياً، فما احتجت ألمس أي منطق تاني. **تحقّقت فعلياً بمتصفح حقيقي:** رصيد بداية المرحلة = 15,000 (فحصت الداتابيز مباشرة)، اختيار الأربعة خدمات مع بعض (19,500) **انرفض فعلياً هالمرة** برسالة "مجموع الاختيارات (19500) أكبر من الحد الأقصى (15000)" — **أول اختبار حقيقي وقابل للتنفيذ لحد الميزانية** (يحل الملاحظة يلي سجّلتها بالسطر يلي قبل هذا عن كون حد الـ30,000 غير قابل للتفعيل إطلاقاً)، تركيبة 3 عناصر (9,500) اشتغلت طبيعي، وتركيبة عند الحد بالضبط (10,000+5,000=15,000) اشتغلت طبيعي كمان (يثبت إن الفحص ≤ مو <)، وإعادة البدء هيّأت الرصيد لـ15,000 من جديد مو 30,000 (تأكدت من الداتابيز بعد إعادة الدخول للمرحلة).

- **2026-09-15 — مرحلة "قرار التمويل" (مرحلة 3، القسم 3):** أول مرحلة تُنشئ رأس المال الأساسي الفعلي (`startingCapital`)، منفصل تماماً عن `marketResearchCredit` يلي خلص دوره. 3 مستويات تمويل (`lib/financing.ts`) بتصميم متطابق بصرياً 100% (radio + نطاق + مهلة + حصة مستثمر، نفس الـJSX عبر `.map()`): محدود (80–120 ألف، 60 يوم، 0%)، متوسط (200–300 ألف، 40 يوم، 10%)، مريح (400–600 ألف، 25 يوم، 25%). اللاعب يدخل رقم حر داخل نطاق المستوى المختار (حقل رقمي، **بدون** attribute `min`/`max` بالـHTML عمداً — كانت بتمنع الإرسال بالكامل لأي رقم خارج النطاق قبل ما توصل السيرفر، وهيك ما كان ممكن نثبت رفض حقيقي من السيرفر، وهو شرط قبول صريح). عند التأكيد (`confirmFinancingDecision`) تُخزّن 4 حقول flat بعمود data (مو متداخلة بكائن، بنفس نمط currentStage): `startingCapital`، `investorEquityPercent` الثابتة حسب المستوى، `financingStartedAt` (توقيت التأكيد الفعلي)، و`financingDeadlineDays`. **عداد تنازلي حي** (`FinancingCountdown.tsx`) بيظهر بكل شاشات `/game` التالية طالما فيه قرار تمويل محفوظ — بيتحسب دايماً من `financingStartedAt + financingDeadlineDays` مقارنة بـ`Date.now()` الفعلي كل ثانية (مو عداد محلي بيصفّر من جديد كل تحميل صفحة)؛ استخدمنا نمط "ابدأ بـnull واحسب أول قيمة جوا الـinterval نفسه" لتفادي Hydration Mismatch (وقت السيرفر ≠ وقت المتصفح) ولتفادي تحذير ESLint عن استدعاء setState مباشر جوا effect. `advanceStage` نفسه محمي سيرفر-سايد لمرحلة 3 كمان (مو بس إخفاء الزر). **تحقّقت من كل بند قبول فعلياً، بما فيها الاختبار الحاسم:** رقم خارج النطاق (500,000 لمستوى نطاقه 80–120 ألف) انرفض فعلياً من السيرفر برسالة واضحة، الأربعة حقول محفوظة صح بالداتابيز (فحص مباشر)، العداد ظاهر فوراً بعد التأكيد. **اختبار "سكّر المتصفح وارجع بعد وقت" عملته بطريقتين:** (1) رجّعت `financingStartedAt` بالداتابيز مباشرة ساعتين للوراء ثم فتحت جلسة جديدة كلياً — العداد نقص بالضبط ساعتين. (2) الاختبار الحرفي يلي طلبتوه: أكّدت التمويل، سكّرت المتصفح بالكامل (`browser.close()`)، انتظرت **191 ثانية فعلية** (مو محاكاة)، فتحت جلسة جديدة كلياً وسجّلت دخول من جديد — العداد نقص **بالضبط 191 ثانية**، مطابق تماماً للوقت الحقيقي المنقضي (مو نفس الرقم القديم كأنه بدأ من جديد). إعادة البدء مسحت الأربعة حقول فعلياً من الصف (تأكدت من الداتابيز — data رجعت `{currentStage: 1}` بس). بيانات الاختبار انحذفت.

- **2026-09-15 — تصحيح: استبدال العداد الحي بالثواني بعرض أيام محاكاة ثابت (فوق نفس مرحلة التمويل، commit جديد بعد 0ac5457):** المستخدم طلب هالتصحيح كـ"تنفيذ طلب سابق" — بس ما عندي أي سجل بمحادثتنا لطلب متل هيك قبل هيك؛ الوصف الأصلي يلي استلمته لمرحلة 3 كان يطلب صراحة عداد وقت حقيقي (`financingStartedAt + financingDeadlineDays` مقارنة بـDate.now())، وهو يلي بنيته واختبرته. بلّغت المستخدم بهاد التناقض بوضوح، وبعدين نفّذت طلبه الجديد كامل زي ما ورد بالضبط. **التغيير:** حذفت `setInterval`/`useEffect`/`Date.now()` بالكامل من `FinancingCountdown.tsx` — صار مكوّن سيرفر عادي (بدون "use client") بياخد `remainingDays`/`totalDays` كـprops جاهزة ويعرض نص ثابت "متبقي X يوم من Y" بس. أضفت `daysConsumed` (عمود data، يبدأ 0)، و`getDaysConsumed`/`consumeGameDays` بـ`lib/db.ts` — `consumeGameDays` جاهزة تُستدعى من منطق لعبة لاحق (لسا ما موجود، ولا مرحلة تستدعيها حالياً) لتنقيص المهلة كتكلفة قرارات، مو وقت حقيقي منقضي. `remainingDays = financingDeadlineDays - daysConsumed` يُحسب بـ`page.tsx` (سيرفر) ويُمرّر جاهز. `daysConsumed` انضاف لقائمة الحقول يلي "إعادة البدء" بتمسحها. **تحقّقت فعلياً (مو تخمين):** بمتصفح حقيقي، الرقم ضل **"متبقي 60 يوم من 60" بالحرف** عبر 3 عمليات `page.reload()` متتالية وعبر انتظار حقيقي (مرتين، 6 ثواني كل مرة) بدون أي تغيير إطلاقاً. استدعيت `consumeGameDays` يدوياً (نفس استعلام SQL يلي بالكود بالضبط، مباشرة على قاعدة البيانات) بزيادة 7 أيام — رجعت لصفحة `/game` بجلسة جديدة وطلع **"متبقي 53 يوم من 60"** بالضبط (60-7=53)، يعني الرقم ما تغيّر إلا لما `consumeGameDays` فعلياً اتنادى، مو غير هيك. إعادة البدء مسحت `daysConsumed` من الصف فعلياً (تأكدت من الداتابيز — `data` رجعت `{currentStage: 1}` بس).

- **2026-09-15 — مرحلة "الترخيص" (مرحلة 4، القسم 3):** أول إنفاق فعلي بالمشروع — أضفنا `currentCapital` (`lib/db.ts`، `getCurrentCapital`): يتهيّأ lazy من `startingCapital` أول زيارة لهالمرحلة ويخزّن فوراً، ومن هون فصاعداً هو يلي يُخصم منه (مو `startingCapital` يلي يضل سجل تاريخي ثابت). مسارين (`lib/licensing.ts`) معروضين بوضوح — التكلفة والمدة معلنة، مو محايدة بصرياً (خلافاً لقاعدة دراسة السوق): (1) شركة/وكيل — 15,000، 10 أيام، بدون مفاجآت. (2) ترخيص بنفسك — أساس 5,000/25 يوم، + checkbox "جاهزية" (لو غير محدد: +5 أيام)، + 3 أحداث عشوائية مستقلة عن بعضها (نواقص بالطلب 35%→+5 أيام بلا تكلفة، كشف ميداني 25%→+3,000/+8 أيام، سلامة/دفاع مدني 20%→+4,000/+6 أيام) تُرمى مرة وحدة وقت التأكيد وتُخزَّن نهائياً. `licensingPath` ("agency"/"self") حقل flat منفصل (لاستخدام مرحلة الإيجار لاحقاً — لسا ما مبني)، والتفاصيل الكاملة بـ`licensingResult` متداخل (نفس نمط `marketResearchResults`). التحقق من كفاية `currentCapital` سيرفر-سايد قبل أي خصم — لو رفض، ولا خصم ولا استهلاك أيام يصير (تأكدنا الصف يضل بدون تغيير). "إعادة البدء" بتمسح `currentCapital`/`licensingPath`/`licensingResult` الثلاثة (بالإضافة لحقول التمويل يلي كانت تُمسح أصلاً). **تحقّقت فعلياً بمتصفح حقيقي (Playwright) من كل بند قبول:** مسار الوكيل خصم بالضبط 15,000 من 100,000 (رجع 85,000) واستهلك بالضبط 10 أيام (فحص مباشر بالداتابيز)؛ الذاتي بدون جاهزية طلع بالضبط +5 أيام فوق الأساس (30 يوم بدون مفاجآت هالمرة)؛ جرّبت 8 محاولات متتالية للمسار الذاتي لحد ما طلعت مفاجأة فعلية ("كشف ميداني")، وتأكدت التكلفة/الأيام النهائية طابقت الحساب بالضبط (5,000+3,000=8,000، 25+8=33) والواجهة عرضت اسم الحدث والتفاصيل صح؛ رفض تجاوز الرصيد جرّبته بفرض `currentCapital=10,000` مباشرة بالداتابيز (بما إن هالمرحلة أول إنفاق، ما في طريقة طبيعية توصل لرصيد أقل من أي تكلفة ترخيص) ثم حاولت أختار الوكيل (15,000) — انرفض فعلياً من السيرفر، ولا `currentCapital` تغيّر ولا `licensingPath` انخزن؛ جلسة جديدة كلياً طلعت نفس النتيجة المحفوظة بالحرف (مو إعادة حساب)؛ إعادة البدء مسحت الحقول الثلاثة فعلياً (تأكدت من الداتابيز — `data` رجعت `{currentStage: 1}` بس). بيانات الاختبار انحذفت.

- **2026-09-15 — مرحلة "الإيجار" (مرحلة 5، الجزء أ فقط، القسم 3):** أربعة خيارات جاهزة (`lib/rent.ts`) — منطقة صناعية/رخيص وعملي (24,000، خدمات كاملة، متوسطة)، رخيص وفاضي (14,000، ناقص، متوسطة)، غالي وموقع مميز (55,000، ناقص، صغيرة، `primeLocation=true`)، غالي وكامل الخدمات (70,000، كامل، كبيرة) — بنفس التصميم البصري بالضبط (كروت متطابقة حرفياً، الفرق بالنص/الأرقام بس؛ التكلفة نفسها معلنة ومو محايدة، خلافاً لقاعدة دراسة السوق). زر "تفاصيل" (بس لخيارَي الفاضي) بيكشف 4 خدمات ناقصة (1,500 لكل وحدة، 6,000 إجمالي) — لو انضغط قبل التأكيد بتنضاف كاملة للسعر المعروض (شفافية كاملة، بدون مفاجأة)؛ لو ما انضغط، السعر المعلن بس يُخصم عند التأكيد وبعده مباشرة تُرمى 4 نردات مستقلة (50% لكل خدمة) كمفاجأة تُخصم فوراً من `currentCapital` وتُعرض برسالة "اكتشفت نواقص". زر "تفاوض" منفصل (مرة وحدة، محمي سيرفر-سايد مو بس بتعطيل الزر) — 60% نجاح يعطي خصم 10%، ولو مع `licensingPath="agency"` يصير 20% + يخزّن `freeSetupDays=15` و`wasteRemovalIncluded=true` (لمراحل لاحقة). الدفع: كاش كامل، أو تقسيط (20% دفعة أولى + الباقي 80% مع علاوة 15% مقسّمة على 12 قسط) بيضيف أول عنصر لمصفوفة جديدة `monthlyObligations` (`{type:"rent", monthlyAmount}`, بدون أي منطق خصم تلقائي). خزّن أيضاً `rentChoice`/`rentSpaceSize`. تحقق `currentCapital` كافي (الدفعة الأولى/الكاش) سيرفر-سايد قبل أي خصم. "إعادة البدء" بتمسح `rentChoice`/`rentSpaceSize`/`rentResult`/`rentNegotiation`/`monthlyObligations`/الأعلام الثلاثة. **تحقّقت فعلياً بمتصفح حقيقي (Playwright) من كل بند قبول:** الأربعة كروت متطابقة حرفياً؛ خيار فاضٍ+تفاصيل قبل التأكيد أعطى سعر شفاف 20,000 (14,000+6,000) بدون أي مفاجأة بعدية؛ نفس الخيار بدون تفاصيل أعطى خصم 14,000 عند التأكيد ثم مفاجأة فعلية بعده (طلعت "مكاتب"+"تنظيف وصيانة"=3,000 هالمرة) مع رسالة "اكتشفت نواقص" على الشاشة؛ تفاوض ناجح (أول محاولة من أصل 10) مع `licensingPath="agency"` أعطى خصم 20% فعلي بالضبط (70,000→56,000، تحقق مباشر بالداتابيز) + `freeSetupDays`/`wasteRemovalIncluded` محفوظين صح؛ تقسيط أنشأ `monthlyObligations` بعنصر واحد صحيح (دفعة أولى 4,800، قسط شهري 1,840 — طابق الحساب بالضبط 24,000×0.8×1.15÷12)؛ رفض تجاوز الرصيد جرّبته بفرض `currentCapital=10,000` مباشرة (نفس أسلوب مرحلة الترخيص) ثم حاولت خيار 70,000 — انرفض فعلياً من السيرفر بدون أي تغيير بالصف؛ جلسة جديدة كلياً طلعت نفس النتيجة المحفوظة بالحرف؛ إعادة البدء مسحت كل الحقول والأعلام فعلياً (`data` رجعت `{currentStage: 1}` بس، بعد تأكيد فعلي لخيار "موقع مميز" وتأكدت `primeLocation=true` كان محفوظ قبل المسح). بيانات الاختبار انحذفت. **ملاحظة تقنية جانبية:** ضفت `argsIgnorePattern: "^_"` لقاعدة `no-unused-vars` بـ`eslint.config.mjs` — الأفعال المربوطة بـ`useActionState` بتستقبل `(prevState, formData)` دايماً حتى لو بعضها ما محتاج أي منهم، وهيك ما نضطر نسيب باراميترات مستخدمة وهمياً بس لتفادي التحذير.

- **2026-09-15 — مرحلة "المعدات" (مرحلة 6، القسم 3):** قرار أولي وحيد لنوع الخط وعدد العمال — بدون نمو إنتاج أو خطوط إضافية (مؤجل). ميزانية مساحة حسب `rentSpaceSize` المحفوظ من مرحلة الإيجار (`lib/equipment.ts`، `SPACE_BUDGET`): صغيرة=8، متوسطة=18، كبيرة=30. خياران متطابقان بصرياً بالضبط: أوتوماتيكي كامل (80,000، 10 وحدة مساحة، عمال مقترح 2، كاش أو تقسيط)، ونصف أوتوماتيكي (35,000، 6 وحدة، عمال مقترح 5، **كاش إجباري بدون أي مسار تقسيط إطلاقاً**). فحص المساحة (سيرفر-سايد صارم — الواجهة بس بتعرض تحذير استرشادي، الحماية الحقيقية بـ`confirmEquipmentDecision`): لو وحدات الخط المختار أكبر من الميزانية المتاحة، رفض فوري بدون أي خصم. عدد العمال رقم حر ≥1 (بدون حد أعلى)، راتب 800/شهرياً لكل عامل — عند التأكيد بيضاف عنصر `{type:"production-workers", monthlyAmount: عدد×800}` لـ`monthlyObligations` **دايماً** (بغض النظر عن طريقة دفع المعدات)، وعنصر `{type:"equipment", monthlyAmount}` إضافي بس لو أوتوماتيكي+تقسيط (نفس صيغة الإيجار: 20% دفعة أولى + علاوة 15% على 12 قسط). خزّن أيضاً `equipmentType`/`equipmentSpaceUsed` لمرحلة الإنتاج لاحقاً. **ملاحظة تصميم (تعارض بين طلبَي مرحلتين سابقتين، حللته بدل ما أتجاهله):** طلب مرحلة الإيجار كان "إعادة البدء تصفّر monthlyObligations" (مسح كامل)، وطلب هالمرحلة كان "إعادة البدء تزيل عنصري equipment/production-workers فقط، دون التأثير على rent" — بما إنه فيه زر "إعادة البدء" واحد بس بيصفّر اللعبة كلها (مو Reset لكل مرحلة لحالها)، والطلبين ما بيصير يتحققوا الاثنين حرفياً بنفس اللحظة النهائية: خليت `resetEquipment` (فلترة جراحية تشيل بس equipment/production-workers وتخلي rent، تماماً متل ما طلبت هالمرحلة) تشتغل **قبل** `resetRent` (يلي بيمسح monthlyObligations بالكامل، متل ما طلبت مرحلة الإيجار) — هيك الدالتين كل وحدة صحيحة ومختبرة لحالها (تأكدت من سلوك `resetEquipment` المعزول بإعادة تنفيذ نفس الـSQL يدوياً)، والنتيجة النهائية لزر "إعادة البدء" الفعلي = لوح نظيف تماماً (`{currentStage:1}` بس) بدون أي التزام مالي معلّق بلا قرار مرتبط فيه. **تحقّقت فعلياً بمتصفح حقيقي (Playwright) من كل بند قبول:** الكرتين متطابقين حرفياً؛ خط أوتوماتيكي مع مساحة "صغيرة" (موقع مميز، 8 وحدة) انرفض فعلياً من السيرفر ("هذا الخط لا يناسب المساحة المستأجرة") بدون أي حفظ؛ **اختبار أمان مهم:** جربت أجبر طلب تقسيط على الخط النصف أوتوماتيكي (يدوياً عبر `page.evaluate` لتغيير قيمة الحقل المخفي، بما إنه ما في أي عنصر واجهة أصلاً يتيح هذا) — انرفض فعلياً من السيرفر ("هذا الخط كاش إجباري")؛ تقسيط الأوتوماتيكي بـ3 عمال طابق الحساب بالضبط (دفعة أولى 16,000، قسط شهري 6,133 = 80,000×0.8×1.15÷12)، وعنصر `production-workers` طابق 3×800=2,400 بالضبط؛ رفض تجاوز الرصيد (فرضت `currentCapital=10,000` والخط يكلف 35,000) انرفض فعلياً بدون أي تغيير بالصف؛ جلسة جديدة كلياً طلعت نفس النتيجة المحفوظة بالحرف؛ وتأكدت من سلوك `resetEquipment` المعزول (نفّذت نفس SQL بتاعتها يدوياً) إنها بتشيل `production-workers` وتخلي `rent` — وبعدها زر "إعادة البدء" الفعلي رجّع الصف لـ`{currentStage:1}` نظيف تماماً. بيانات الاختبار انحذفت.

- **2026-09-15 — مرحلة "التوظيف" (مرحلة 7، القسم 3):** دوران مستقلان تماماً عن بعض — كيميائي/فورميوليتور ومحاسب — كل واحد فيهم 3 خيارات متطابقة بصرياً 100% (جونيور/سينيور/"لا توظف"، بنفس بنية الكرت بالضبط عبر `.map()` واحد على `HIRING_ROLES`، بما فيها "لا توظف" يلي عندها نفس شكل الكرت وزر "تفاصيل"): كيميائي جونيور 2,500/شهرياً، سينيور 5,500/شهرياً؛ محاسب جونيور 2,000/شهرياً، سينيور 4,500/شهرياً. الراتب ظاهر افتراضياً بكل كرت؛ زر "تفاصيل" اختياري بيكشف نص خبرة قصير (سيرة واقعية 1-2 جملة) مختلف للأربعة مرشحين الفعليين — النص غايب من الـDOM بالكامل قبل الضغط (مو بس مخفي بـCSS). **بدون أي فحص `currentCapital`** (راتب = التزام شهري مستقبلي، مو خصم فوري — تعليمة صريحة من المستخدم لهالمرحلة تحديداً، خلافاً لكل مراحل الإنفاق السابقة). عند التأكيد (`confirmHiringDecision`، الدورين مع بعض بنفس الطلب): كل دور تم توظيفه بيضيف عنصر لـ`monthlyObligations` (`{type:"salary-chemist"|"salary-accountant", monthlyAmount}`) ويخزّن `chemistHired`/`accountantHired: true` + `chemistExperience`/`accountantExperience: "junior"|"senior"`؛ "لا توظف" ما بيضيف أي التزام ويخزّن `false`/`null`. زر "التالي" ما بيظهر (سيرفر-سايد بـ`advanceStage` نفسه، مو بس إخفاء واجهة) إلا بعد ما الدورين الاثنين يكون عندهم قرار محفوظ (حتى "لا توظف" لهالدورين يعتبر قرار كافي). **لا نظام أخطاء عشوائية مبني هون** (يحتاج دورات إنتاج/محاسبة لسا ما مبنية) — بس `chemistExperience`/`accountantExperience` مخزّنين جاهزين لهيك نظام لاحقاً. "إعادة البدء" (`resetHiring`) بتشيل بس الأربعة حقول (`chemistHired`/`chemistExperience`/`accountantHired`/`accountantExperience`) وبس عنصري `salary-chemist`/`salary-accountant` من `monthlyObligations` (فلترة جراحية بنفس نمط `resetEquipment` بمرحلة 6) — مرتّبة قبل `resetRent` بـ`restartGame()` (نفس منطق الترتيب المحلول بمرحلة 6: الدوال الجراحية أولاً، `resetRent` يلي بيمسح المصفوفة بالكامل أخيراً، فالنتيجة النهائية لزر "إعادة البدء" الفعلي تضل لوح نظيف تماماً). **تحقّقت فعلياً بمتصفح حقيقي (Playwright) + فحص مباشر لقاعدة البيانات من كل بند قبول:** الستة كروت (3×دورين) متطابقة حرفياً بالـclassName؛ الأربعة رواتب ظاهرة افتراضياً بالنص؛ نص خبرة الكيميائي السينيور غايب تماماً من HTML قبل الضغط على "تفاصيل"، وظهر بعد الضغط بدون ما يأثر على غياب نصوص باقي الكروت؛ سينيور+سينيور أنشأ بالضبط عنصرين صحيحين (`salary-chemist:5500`, `salary-accountant:4500`) مع الحفاظ الكامل على عناصر `rent`/`equipment`/`production-workers` الموجودة مسبقاً؛ لا توظف+لا توظف أنشأ صفر التزامات جديدة وخزّن `false`/`null` للدورين بوضوح؛ حالة مختلطة (جونيور كيميائي + لا توظف محاسب) أثبتت استقلالية الدورين فعلياً (التزام واحد بس، بالقيمة الصح)؛ زر "التالي" غايب قبل التأكيد وظاهر بعده بجلسة جديدة كلياً (`page.reload()` + تسجيل دخول من جلسة منفصلة تماماً)؛ `resetHiring` بمعزل (نفّذت نفس SQL بتاعتها يدوياً) شالت الأربعة حقول وعنصري الراتب بس، وخلّت `rent`/`equipment`/`production-workers` بدون أي تغيير؛ فحص `advanceStage` تأكد إن `currentStage` ما بيتحرك من 7 طالما القرار غير مكتمل. بيانات الاختبار (مستخدمين تجريبيين + صفوفهم بـ`neon_auth`/`game_state`) انحذفت بالكامل بعد الاختبار.

- **2026-09-17 — مرحلة "الإنتاج" (مرحلة 8، الجزء أ فقط، القسم 3):** أول مرحلة متكررة (دورات) قبل الانتقال للمرحلة التالية — بدون معادلة كمية/جودة، تنويع، تعبئة، أخطاء عشوائية، أو QC (كلهم أجزاء لاحقة). حقل جديد دائم `rawMaterialInventory` (يبدأ 0، يتراكم عبر الشراء ويُستهلك بالإنتاج) وحقل `productionCycles` (مصفوفة، كل عنصر `{cycleNumber, supplierChoice, purchaseQuantity, purchaseCost, producedUnits}` — بدون حذف الدورات السابقة). `productionCapacity` (`lib/production.ts`) دالة بس بـ`equipmentType`/`workerCount` المحفوظين من مرحلة المعدات (6): أوتوماتيكي = 60+عمال×20، نصف أوتوماتيكي = 25+عمال×12 — تُعرض للاعب بوضوح مع المخزون الحالي بداية كل دورة. شراء كل دورة: مورّد رخيص (12/وحدة، بدون علم خطر بعد — مؤجل لجزء ج) أو موثوق (20/وحدة) + كمية حرة ≥0؛ خصم 15% تلقائي لو (الكمية المشتراة + المخزون الحالي) ≥ 1.2×الطاقة ("شراء بالجملة"). عند التأكيد: خصم فوري من `currentCapital` (رفض سيرفر-سايد لو ما يكفي، بدون أي تغيير لو رُفض)، ثم إنتاج تلقائي فوري بدون خطوة منفصلة: `producedUnits = min(المخزون بعد الشراء, الطاقة)`، والباقي يضل بالمخزون للدورة الجاية. زر "دورة إنتاج جديدة" (`ProductionStage.tsx`) يرمونت النموذج الداخلي بمفتاح جديد بدل `useEffect` لمزامنة حالة مشتقة (تفادياً لتحذير ESLint `react-hooks/set-state-in-effect`) ويرجع لنموذج شراء جديد بدون مغادرة المرحلة. زر "الانتقال لمرحلة البيع" هو نفسه زر "التالي" العام الموجود أصلاً بـ`page.tsx` — يظهر تلقائياً بمجرد `productionCycles.length > 0` (سيرفر-سايد بـ`advanceStage` نفسه كمان، مو بس إخفاء واجهة). "إعادة البدء" (`resetProduction`) تصفّر الحقلين إلى `[]`/`0` صراحة (قيم، مو حذف مفاتيح — خلافاً لبقية دوال Reset السابقة) حسب الطلب الصريح لهالمرحلة. **تحقّقت فعلياً بمتصفح حقيقي (Playwright) + فحص مباشر لقاعدة البيانات من كل بند قبول:** أوتوماتيكي+3 عمال أعطى طاقة 120 ظاهرة بالضبط (60+3×20)، نصف أوتوماتيكي+5 عمال أعطى 85 (25+5×12)؛ شراء 120 وحدة موثوقة بطاقة 100 (عتبة 120) فعّل الخصم فعلياً (2400→2040 بالضبط)، وشراء 119 (أقل من العتبة) ما فعّله (2380 بدون خصم)؛ عبر دورتين متتاليتين: دورة 1 (شراء 150، طاقة 100) أنتجت بالضبط 100 وسيّبت مخزون 50 (حالة مخزون أكبر من الطاقة)، دورة 2 (بدون شراء إضافي، الاعتماد على المخزون المتبقي) أنتجت بالضبط 50 = min(50,100) (حالة مخزون أقل من الطاقة) وصفّرت المخزون — والمصفوفة صار طولها 2 بأرقام دورات صحيحة (1، 2) دون حذف الأولى؛ زر "التالي" غايب قبل أي دورة وظاهر بعد أول دورة، والضغط عليه نقل `currentStage` فعلياً لـ9؛ رفض تجاوز `currentCapital` (فرضت رصيد 100 وحاولت شراء بـ1000) رجع رسالة رفض فعلية بدون أي تغيير على الرصيد أو المصفوفة أو المخزون؛ جلسة جديدة كلياً عرضت نفس `rawMaterialInventory`/`productionCycles` بدون إعادة حساب؛ "إعادة البدء" صفّرت `productionCycles` لـ`[]` و`rawMaterialInventory` لـ0 فعلياً (تأكدت من الداتابيز). **ملاحظة تصحيح أثناء الاختبار:** أول تشغيل لاختبار "إعادة البدء" فشل (المصفوفة القديمة ضلت موجودة) — تبيّن إنه تايمنغ بالاختبار نفسه مو خلل بالتطبيق: `restartGame()` بينفّذ 7 عمليات `UPDATE` متتالية (مو معاملة SQL وحدة)، والاختبار كان بيقرا الداتابيز بعد 1,500ms بس بينما التنفيذ الفعلي أخذ ~1,800ms+ (سجلات السيرفر أكدت هيك) — أعدت اختبار `resetProduction` بمعزل (بإعادة تنفيذ نفس الـSQL يدوياً على صف حقيقي) فأكّد إنها صحيحة 100%، وبعد ما رفعت مهلة الانتظار بالاختبار لـ3,000ms صارت كل الاختبارات (بما فيها هاد) ناجحة بثبات على تشغيلتين متتاليتين. بيانات الاختبار (مستخدمين تجريبيين + صفوفهم بـ`neon_auth`/`game_state`، بالإضافة لصف تجريبي يتيم من جلسة سابقة كان لسا موجود بالداتابيز) انحذفت بالكامل.

- **2026-09-17 — مرحلة "الإنتاج" (مرحلة 8، الجزء ب، القسم 3، فوق commit 26e10ee):** استبدلت معادلة الإنتاج القديمة (`min(inventory, capacity)`) بالكامل بمعادلة سرعة/دقة حقيقية، وأضفت خط منتج + توزيع تعبئة — كلهم قرارات جديدة داخل نفس نموذج تأكيد الشراء الموجود، بدون خطوة منفصلة. `processingMode` ("fast"→quality:"low"، "precise"→quality:"high") يحدد `maxRawUsable` (fast: `floor(capacity/2)`، precise: `capacity×2`) و`rawMaterialConsumed = min(المخزون المتاح, maxRawUsable)`، و`producedUnits` (fast: `floor(consumed×2)`، precise: `floor(consumed/2)`) — الباقي غير المستهلك (`purchaseQuantity - rawMaterialConsumed`، مو الكمية كلها) يتراكم فعلياً للدورة الجاية (نفس سلوك تراكم الجزء أ بالضبط، بس بمعادلة استهلاك مختلفة الآن). `productLineType` ("single"/"diversified") يُخزَّن بدون أي أثر فعلي بالنتيجة الآن — جاهز لمرحلة البيع لاحقاً (فتح قناة "تجار صغار"). `packagingSplit`: حقلان `smallPercent`/`largePercent` يُرفضان سيرفر-سايد لو مجموعهما ≠ 100 بالضبط؛ `smallUnits = round(producedUnits×smallPercent/100)` و`largeUnits = producedUnits - smallUnits` (حساب الفرق مباشرة، مو معادلة منفصلة، لضمان التطابق التام حتى بنسب فردية مسبّبة للتقريب زي 33%/67%). كل عنصر `productionCycles` صار يحمل إضافة لحقول الجزء أ: `processingMode`, `quality`, `rawMaterialConsumed`, `productLineType`, `smallUnits`, `largeUnits`. **تحقّقت فعلياً بمتصفح حقيقي (Playwright) + فحص مباشر لقاعدة البيانات من كل بند قبول:** نفس المخزون (10 وحدات، أقل من سقف الوضعين) بوضعين منفصلين أعطى fast=20 وحدة وprecise=5 وحدة (`rawMaterialConsumed=10` بالحالتين، مطابق للصيغ بالضبط)؛ عبر دورتين متتاليتين بوضعين مختلفين على نفس اللاعب: دورة 1 (fast، شراء 80، طاقة 100) استهلكت 50 بالضبط (`min(80, floor(100/2))`) وأنتجت 100، وسيّبت مخزون 30 متراكم؛ دورة 2 (precise، بدون شراء إضافي، بالاعتماد على الـ30 المتراكمة) استهلكت الـ30 كاملة وأنتجت 15 (`floor(30/2)`) دون التأثير على بيانات الدورة الأولى المحفوظة؛ رفض نسب 60%+30% (≠100) رجع رسالة رفض فعلية بدون أي خصم أو إضافة دورة؛ نسب فردية 33%/67% على 15 وحدة أعطت `smallUnits=5, largeUnits=10` بمجموع مطابق تماماً (بدون فرق تقريب)؛ `productLineType` محفوظ صح لكل دورة (diversified بدورة 1، single بدورة 2)؛ جلسة جديدة كلياً عرضت نفس `productionCycles` بكل حقولها الجديدة بدون إعادة حساب؛ "إعادة البدء" ضلت تصفّر `productionCycles`=[] و`rawMaterialInventory`=0 فعلياً رغم الحقول الإضافية. **ملاحظة أمانة صريحة (تناقض بين نص شرط القبول والصيغ المعطاة):** شرط القبول ذكر "معالجة fast تعطي **ضعف** عدد الوحدات المنتجة مقارنة بـprecise" لنفس المخزون — لكن الصيغ الحرفية المعطاة (fast=`2×consumed`، precise=`0.5×consumed`) تعطي رياضياً **نسبة 4× وليس 2×** لنفس قيمة `rawMaterialConsumed` (لأن fast تعطي ضعف المستهلك بينما precise تعطي نصفه — الفرق بينهم ضرب أربعة). اختبرت الرقمين فعلياً (10 وحدات مخزون بالحالتين → fast=20، precise=5 → نسبة 4×) ونفّذت الصيغ حرفياً كما طُلب صراحة بدل ما أعدّل الأرقام لتصطنع "ضعف" غير موجود فعلياً بالرياضيات — بلّغتكم بدل ما أفترض. بيانات الاختبار انحذفت بالكامل.

- **2026-09-17 — حادثة أمان بيانات + بناء بيئة التطوير المعزولة (القسم 8، متأخر — كان المفروض أول شي بالمشروع):** أثناء تشخيص خلل باختبار الجزء ج من مرحلة الإنتاج (لسا غير مكتمل، بانتظار الاستئناف)، نفّذت استعلام SQL معزول لأخذ "أي" `user_id` جاهز (`SELECT id FROM neon_auth."user" LIMIT 1` — بدون شرط تصفية) بدل ما أحدد حساب اختبار بعلامة واضحة. الصف يلي رجع كان **حساب المستخدم الحقيقي نفسه** (`cahn1986m@gmail.com`) — كتبت فوق `game_state` تبعه ببيانات اختبار، وبعدين صفّرته لـ`{}` بمحاولة "تنظيف". أبلغت المستخدم فوراً بالضبط لما اكتشفت الخطأ (قبل أي محاولة إخفاء أو تبرير)، وأكّد إنه غالباً كان فيه تقدّم حقيقي (كان يستخدم الحساب للتحقق اليدوي بعد كل مرحلة) — انفقد بدون نسخة احتياطية، بس الأثر محدود لأنها بيانات تجربة شخصية بمرحلة ما قبل الإطلاق مو بيانات زبون حقيقي. **القرار: توقف فوري عن أي بناء ميزات جديدة، وبناء البنية التحتية يلي كان مفروض تُبنى بالجلسة الأولى (القسم 8 من roadmap.md: "فرع تطوير منفصل بقاعدة بيانات نيون... معزول تماماً عن أي بيانات حقيقية مستقبلية") لكن ما اتبنت فعلياً — كل المراحل لحد الآن (1 إلى 8 جزء ب) اشتغلت وتحقّقت مباشرة ضد `main` (قاعدة الإنتاج الحقيقية).** المستخدم فضّل ينشئ الفرع بنفسه من لوحة تحكم Neon (بدل ما يزوّدني مفتاح Neon API لأتمتة الإنشاء) وأعطاني connection string فرع `development` الجديد. حدّثت `.env.local` (`DATABASE_URL` و`NEON_AUTH_BASE_URL` معاً — لاحظت إن Neon Auth كمان مرتبط بالـendpoint المحدد للفرع، مو مشترك بين الفروع، فتحقّقت فعلياً إن فرع `development` عنده خدمة Neon Auth شغالة لحاله قبل ما أعتمد الرابط). **تحقّقت من العزل الحقيقي بطريقتين:** (1) كتبت علامة (marker) بجدول مؤقت على فرع `development` بس، وتأكدت `main` ما شافها إطلاقاً (الجدول أصلاً مش موجود عليه) — إثبات إنهم قاعدتا بيانات منفصلتان فعلياً مو مجرد رابطين لنفس المكان؛ (2) شغّلت السيرفر المحلي بعد تحديث `.env.local`، سجّلت حساب حقيقي جديد عبر متصفح فعلي (Playwright)، وتأكدت الحساب انخزن على `development` وغائب تماماً عن `main`. كمان لقيت ولحّقت بيانات اختبار يتيمة (يوزرين `prodc-test-junior-rate-...`) كانت لسا موجودة على `main` من جلسة التشخيص السابقة قبل إنشاء الفرع — انحذفت من `main` مباشرة (بشرط email صريح، مو استعلام عام). **من الآن فصاعداً: كل عمل محلي (`npm run dev`) واختبارات Playwright/SQL تشتغل حصراً على فرع `development`؛ `main` ما يُلمس محلياً إطلاقاً، ويوصل له بس عبر متغيرات بيئة Vercel وقت النشر الفعلي.** أضفت قاعدة صارمة دائمة لنفسي (محفوظة بنظام الذاكرة): أي استعلام يلمس جدول فيه بيانات مستخدمين حقيقيين لازم يحدد الحساب بشرط `WHERE` صريح على نمط بريد اختباري معروف — ممنوع نهائياً أي استعلام "أول صف" أو بدون شرط تصفية.

- **2026-09-17 — مرحلة "الإنتاج" (مرحلة 8، الجزء ج — الأخير، القسم 3، فوق commit 8e67774؛ أول جزء يُبنى ويُختبر على فرع `development` المعزول بالكامل):** هدر طبيعي ثابت + خطأ كيميائي عشوائي + QC اختياري — بهذا تكتمل مرحلة الإنتاج بأجزائها الثلاثة (أ+ب+ج). ترتيب الحساب حرفياً كما طُلب: (1) `afterNaturalWaste = floor(producedUnits × 0.97)` — يُطبَّق دايماً بلا استثناء؛ (2) خطأ الكيميائي احتمالي حسب `chemistHired`/`chemistExperience` المخزّنين فعلياً من مرحلة التوظيف (7) — جدول: بدون كيميائي 35% احتمال/20% هدر إضافي، جونيور 20%/15%، سينيور 8%/8% (`getChemistTier`/`rollChemistError`/`applyChemistErrorWaste` بـ`lib/production.ts`) — عند الحدوث `finalProducedUnits = floor(afterNaturalWaste × (1-wastePercent))`، وإلا `= afterNaturalWaste`؛ (3) إعادة حساب `smallUnits`/`largeUnits` من `finalProducedUnits` (مو `producedUnits` الخام) بنفس نمط "الفرق مباشرة" من الجزء ب. QC اختياري (checkbox بنفس نموذج التأكيد): تكلفة ثابتة 300 تُخصم فوراً مع تكلفة المواد بضربة واحدة (رُفض التأكيد سيرفر-سايد لو المجموع الكامل أكبر من `currentCapital`، مو تكلفة المواد لوحدها)؛ لو مفعّل، نص تقرير صريح يُعرض بعد التنفيذ ("لا توجد مشكلة" أو "اكتُشفت مشكلة — تلف X% من الدفعة"). كل عنصر `productionCycles` صار يحمل إضافة لحقول الجزء أ/ب: `chemistErrorOccurred`, `qcPurchased`, `finalProducedUnits` (المصدر الوحيد للحقيقة لعدد الوحدات من هون فصاعداً). **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات) من كل بند قبول:** معدل خطأ الكيميائي — **60 استدعاء حقيقي متتالي لـ`confirmProductionPurchase` عبر خادم فعلي** (مو محاكاة/قراءة كود) بمستوى جونيور (نسبة محددة 20%) أعطى معدل فعلي 25% (0/60 محاولات فشلت بالإرسال)، ضمن حدود إحصائية 3σ حول النسبة المحددة (±15.5 نقطة عند n=60) — بالإضافة لتحقق رياضي منفصل (200,000 محاولة بنفس صيغة `Math.random() < errorChance` المطابقة حرفياً لثوابت الكود المصدري) أثبت تقارب دقيق جداً (فرق ≤0.06 نقطة) للثلاثة مستويات؛ الهدر الطبيعي 3% تأكد يُطبَّق حتى بدون أي خطأ كيميائي (سينيور، دورة بلا خطأ: `finalProducedUnits=97` من `producedUnits=100` بالضبط)؛ `smallUnits+largeUnits = finalProducedUnits` بالضبط (تحقق بحالة فعلية فيها خطأ كيميائي حدث: 25+52=77)؛ رفض QC لعدم كفاية الرصيد للمجموع الكامل (مواد+300) ظهر فعلياً بدون أي أثر جانبي؛ نص تقرير QC طابق `chemistErrorOccurred` الفعلي بدون أي اختلاف عبر الـ60 دورة (كلا الحالتين: مشكلة/لا مشكلة شوهدتا فعلياً)؛ جلسة جديدة كلياً عرضت نفس `productionCycles` بحقولها الثلاثة الجديدة بدون إعادة حساب؛ "إعادة البدء" ضلت تصفّر `productionCycles`=[] و`rawMaterialInventory`=0. **ملاحظة تصحيح أثناء الاختبار:** أول محاولتين للحلقة الإحصائية (60 دورة على نفس اللاعب) فشلتا جزئياً — تبيّن إنه تايمنغ بالاختبار نفسه مو خلل بالتطبيق: كل الطلبات كانت فعلياً بترجع 200 من السيرفر (تأكدت من سجلات dev)، بس وقت الاستجابة كان يكبر تدريجياً (2.6ث→3.6ث) كل ما تراكمت عناصر أكتر بـ`productionCycles` (حمولة JSONB أكبر بكل قراءة/كتابة)، ومهلة الانتظار الثابتة بالاختبار (1.5ث) صارت غير كافية بعد عشرات الدورات — صحّحت الاختبار ليستنى نتيجة فعلية (ظهور "دورة إنتاج جديدة" أو رسالة خطأ) بدل مهلة ثابتة، وبعدها نجحت الـ60 دورة بالكامل بدون أي فشل. **بيانات الاختبار (كل المستخدمين التجريبيين وصفوفهم) انحذفت بالكامل من فرع `development` — الفرع نفسه، مو `main`، كما تأكد بالحادثة السابقة.**

## مرحلة "الإنتاج" (مرحلة 8) — مكتملة بأجزائها الثلاثة (أ+ب+ج). الخطوة التالية حسب roadmap.md: مرحلة "البيع" (مرحلة 9).

- **2026-09-17 — تصحيح رجعي: ربط استهلاك الأيام (`consumeGameDays`) بمراحل الإيجار (5)، المعدات (6)، والإنتاج (8) (فوق commit ce621f8، على فرع `development`):** فجوة اكتُشفت — `consumeGameDays`/`daysConsumed` انبنت من مرحلة التمويل (3) كبنية جاهزة، بس معظم المراحل اللي بنيت بعدها (الإيجار، المعدات، التوظيف، الإنتاج) ما كانت تستدعيها إطلاقاً، فمهلة الأداء (financingDeadlineDays) كانت عملياً بلا معنى — ما في أي قرار حقيقي يستهلك منها. هالبرومبت يصحّح ثلاث مراحل مبنية ومختبرة سابقاً (الإيجار، المعدات، الإنتاج) **بدون لمس أي منطق آخر فيهم** (الخصومات، فحص المساحة، القيود المالية ضلت بالضبط متل ما هي — تحقق مباشر). **الإنتاج (8):** `cycleDays = max(2, ceil(purchaseQuantity/50))` — يُستدعى `consumeGameDays` فيه لكل دورة، ويُخزَّن كحقل جديد `daysConsumedThisCycle` بعنصر الدورة نفسه (`getProductionCycleDays` بـ`lib/production.ts`). **الإيجار (5):** `baseSetupDays=20` (`RENT_BASE_SETUP_DAYS` بـ`lib/rent.ts`)، ينخفض لـ5 (20-15) لو `freeSetupDays===15` محفوظ فعلياً (تفاوض ناجح سابق مع مسار ترخيص agency) — يُستدعى مرة وحدة عند التأكيد النهائي بس (مو عند فتح "تفاصيل" أو التفاوض نفسه؛ أضفت `getRentFreeSetupDays` بـ`lib/db.ts` لقراءته). **المعدات (6):** `setupDays` صار حقل مباشر بكل خيار بـ`EQUIPMENT_OPTIONS` (أوتوماتيكي=10، نصف أوتوماتيكي=5) بدل قيمة منفصلة بالأكشن. القاعدة المشتركة: `consumeGameDays` يُستدعى **بعد** نجاح كل `apply*Decision`/`applyProductionCycle` مباشرة (نفس مسار التأكيد الناجح الوحيد) — ما بيتكرر عند `reload` (السيرفر أكشن ما بينعاد استدعاؤه من GET عادي) وما بيُستدعى إطلاقاً لو الطلب انرفض لأي سبب (رصيد، مساحة...) لأنه موضوع بعد نقطة الرفض بالكود مباشرة. **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات) من كل بند قبول:** دورة إنتاج بكمية 120 استهلكت 3 أيام بالضبط (`ceil(120/50)`)، ودورة تالية بكمية 30 على نفس اللاعب استهلكت الحد الأدنى 2 (مو `ceil(30/50)=1`) فوصل الإجمالي لـ5 — تحقق مباشر لـ`daysConsumed` قبل وبعد كل دورة؛ تأكيد إيجار مع `freeSetupDays=15` (مزروعة مع `discountPercent=20`، نفس التركيبة الواقعية يلي بيرجّعها `negotiateRentPrice` الحقيقي مع مسار agency) استهلك 5 أيام بالضبط، والسعر النهائي طابق حساب الخصم الأصلي (24,000×0.8=19,200) دون أي كسر؛ تأكيد إيجار بدون العلم استهلك 20 يوم كاملة؛ معدات أوتوماتيكي استهلكت 10، نصف أوتوماتيكي استهلكت 5 (جلستين منفصلتين)؛ رفض معدات لعدم كفاية المساحة (فحص موجود مسبقاً) ضل يعمل تماماً كما هو بدون أي تغيير، و`daysConsumed` ضل 0 (ما استُهلك عند الرفض)؛ إعادة تحميل صفحة بعد كل تأكيد (إيجار، معدات، إنتاج) أكدت `daysConsumed` ما تضاعف. **ملاحظة تصحيح أثناء الاختبار (نفس نمط الجزء ج):** أول محاولتين فشلتا بقراءة `daysConsumed` فوراً بعد الضغط على "تأكيد" (طلعت 0 بدل القيمة الصح، وصارت صحيحة فقط بعد `reload`) — تبيّن إنه تايمنغ اختبار بحت (خادم dev بـTurbopack طلع وقت تحويل بارد كبير جداً لأول استدعاء لكل Server Action بجلسة سيرفر جديدة تماماً، مو خلل تطبيق)؛ صحّحت الاختبار ليستقصي (`poll`) حالة قاعدة البيانات الفعلية مباشرة بمهلة سخية (45 ثانية) بدل الاعتماد على أي إشارة توقيت من المتصفح — وبعدها نجحت كل السيناريوهات بثبات تام. بيانات الاختبار انحذفت بالكامل من فرع `development`.

- **2026-09-17 — تعديل شريط الحالة الدائم (HUD): عرض currentCapital + ضغط المستثمر عند تجاوز المهلة (فوق commit 73e8cd2، على فرع `development`):** جزءان بنفس المكوّن الموجود (`FinancingCountdown.tsx`)، بدون أي مكوّن منفصل جديد. **(1) currentCapital:** يُقرأ الآن دايماً (بس لو فيه قرار تمويل محفوظ — نفس شرط ظهور عداد الأيام أصلاً) عبر `getCurrentCapital` بـ`page.tsx`، ويُعرض بجانب عداد الأيام بنفس الشريط ("💰 X درهم  ⏳ متبقي Y يوم من Z") — بما إن `getCurrentCapital` بتهيّئ (lazy) القيمة من `startingCapital` أول استدعاء، شلت الاستدعاء المكرر يلي كان جوا `isLicensingStage` بس (صار زائد، القراءة المشتركة الجديدة بتغطيه). **(2) ضغط المستثمر:** `isOverdue = daysConsumed > financingDeadlineDays` يُحسب بـ`page.tsx` ويُمرَّر كـprop، مع `investorEquityPercent` (موجود أصلاً بـ`financingDecision`). لما `isOverdue=true`: خلفية الشريط تتحول لأحمر خفيف (بدل الكهرماني المعتاد) + رسالة نصية تتصاعد حسب `investorEquityPercent` (جدول `INVESTOR_PRESSURE_MESSAGES` جديد بـ`lib/financing.ts`: 0="البنك يتابع الوضع"، 10="المستثمرون بدأوا يسألون"، 25="ضغط حقيقي من المستثمرين") — **عرض واجهة فقط، بدون أي حجب فعلي لأي زر أو إجراء** (حسب roadmap.md: "لا اعتراض فعلي على قرارات اللاعب بهذه النسخة")؛ لا عتبات تدرّج إضافية، رسالة واحدة تظهر/تختفي حسب تجاوز الحد فقط. **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات) من كل بند قبول:** currentCapital ظهر صح بثلاث مراحل مختلفة (إيجار/معدات/إنتاج) بنفس الشريط مع أيقونتي 💰 و⏳ معاً؛ `daysConsumed == financingDeadlineDays` بالضبط (مو أكبر) لسا ما بيطلع أي رسالة تحذير (يثبت إن الفحص `>` صح مو `>=`)؛ الثلاث رسائل تحققت منفصلة (investorEquityPercent=0/10/25) مع الخلفية الحمراء (`bg-red-50`) ظاهرة فعلياً على الشريط بكل حالة؛ نموذج المرحلة (الإيجار) ضل تفاعلي بالكامل بدون أي حجب رغم تجاوز المهلة؛ إعادة تحميل الصفحة تبعت تغيير `daysConsumed` مباشر بقاعدة البيانات (فوق الحد ثم تحته) أظهرت/أخفت الرسالة بدقة حسب الحالة الفعلية بكل مرة. بيانات الاختبار انحذفت بالكامل من فرع `development`.

- **2026-09-18 — مرحلة "البيع" (مرحلة 9، الجزء أ فقط، القسم 3، فوق commit b67c443، على فرع `development`):** بيع يدوي متكرر بكمية يختارها اللاعب — بدون مصادر زبائن (الموقع/موظف مبيعات/إعلانات، الجزء ب) أو تسهيلات تجار الجملة (الجزء ج). حقلان جديدان دائمان لكنهما **غير مخزَّنين إطلاقاً** كحقول ثابتة — `availableSmallUnits`/`availableLargeUnits` يُحسبان ديناميكياً عند كل عرض من `productionCycles + salesTransactions` (`getAvailableUnits` بـ`lib/sales.ts` الجديد): مجموع smallUnits/largeUnits لكل دورات الإنتاج مطروحاً منه ما انباع فعلياً حسب القناة، تفادياً لأي تعارض تزامن. حقل جديد `salesTransactions` (مصفوفة، كل عنصر `{transactionNumber, channel, unitsSold, revenuePerUnit, totalRevenue}`). ثلاث قنوات (`CHANNEL_MARGIN`): أسواق تخفيضات (تسحب من availableLargeUnits، هامش 15%)، تجار جملة (نفس المصدر، هامش 25%، كاش فقط بهالجزء)، تجار صغار (تسحب من availableSmallUnits، هامش 60%، **مقفولة سيرفر-سايد بالكامل إلا لو دورة إنتاج واحدة على الأقل كانت `productLineType="diversified"` فعلياً** — فحص مباشر على `productionCycles`، مو علم واجهة). تكلفة الوحدة الفعلية (`computeUnitCost`) = إجمالي `purchaseCost` لكل الدورات ÷ إجمالي `finalProducedUnits` لكلهم — **رقم يُحسب ويُثبَّت مرة وحدة بس عند أول عملية بيع** (`saleUnitCost` حقل جديد بـ`data`)، والعمليات التالية تعيد استخدام نفس القيمة المخزَّنة بدل إعادة الحساب. `revenuePerUnit = round(unitCost × (1+هامش القناة))`، `totalRevenue = unitsSold × revenuePerUnit` يُضاف فوراً لـ`currentCapital` عند التأكيد (`applySaleTransaction` بـ`lib/db.ts`، نفس نمط `applyProductionCycle` الذري: زيادة currentCapital + إضافة عنصر جديد لـsalesTransactions + دمج saleUnitCost لو أول مرة، بضربة UPDATE وحدة). "الانتقال لمرحلة النتيجة" هو زر "التالي" العام، محمي سيرفر-سايد بـ`advanceStage` (نفس نمط الإنتاج) — يتطلب عملية بيع واحدة مكتملة على الأقل. "إعادة البدء" (`resetSales`) تصفّر `salesTransactions` إلى `[]` (قيمة صريحة) وتمسح `saleUnitCost` (كاش مرتبط ببيانات إنتاج بتنمسح هي نفسها، لازم يُعاد حسابه بجولة جديدة). **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات) من كل بند قبول:** بيع تجار صغار بدون أي دورة diversified انرفض سيرفر-سايد فعلياً حتى بعد تجاوز الواجهة مباشرة (فعّلت الـradio المعطّل يدوياً عبر `page.evaluate` وحاولت الإرسال — الرفض صار من السيرفر نفسه، مو الواجهة)؛ تدفق بيع كامل بثلاث دورات متتالية (discount-market 30 وحدة، wholesaler 60 وحدة، boutique-trader 20 وحدة) على بيانات إنتاج حقيقية (تكلفة إجمالية 1,500 ÷ 150 وحدة = تكلفة وحدة 10 بالضبط) أعطى أسعار صحيحة رياضياً لكل قناة (12/13/16 على التوالي — round(10×1.15/1.25/1.6))، و`currentCapital` تراكم صح عبر الثلاث عمليات (+360 ثم +780 ثم +320)؛ بيع بعد استهلاك كل availableLargeUnits (90 كاملة عبر بيعتين) انرفض سيرفر-سايد فعلياً برسالة واضحة بدون أي أثر جانبي، وبرضه بيع تجار صغار يتجاوز الباقي الفعلي (40) بعد أول عملية بيع منها؛ `saleUnitCost` اتخزّن فعلياً عند أول عملية بيع (10) وضل ثابت بلا تغيير عبر باقي العمليات؛ جلسة جديدة كلياً عرضت نفس `salesTransactions` المحفوظة بدون إعادة حساب، وحسبت `availableLargeUnits` من جديد بشكل صحيح (90-10=80)؛ محاولة الانتقال لمرحلة 10 قبل أي بيع رُفضت فعلياً (زر التالي غايب، `currentStage` ضل 9)؛ "إعادة البدء" صفّرت `salesTransactions` إلى `[]` ومسحت `saleUnitCost` فعلياً. **ملاحظة تصحيح مهمة أثناء الاختبار (سباق حقيقي بالاختبار نفسه، مو بالتطبيق):** أثناء بناء اختبار "تدفق بيع كامل" (عدة عمليات بيع متتالية على نفس اللاعب بنفس نموذج التأكيد المستمر، بدون remount بين كل عملية) واجهت فشل متكرر وحاسم بالضبط عند العملية الثانية — تبيّن بعد تشخيص مركّز (سكربت منفصل بمراقبة صريحة لحالة الـradio/الإدخال وطلبات الشبكة) إن التطبيق نفسه **يعمل صحيحاً 100%** بتفاعلات Playwright تسلسلية بسيطة (نقرة → تعبئة → إرسال، مهلة ثابتة سخية)، والمشكلة كانت محاولاتي المتتالية لـ"تحسين" الاستقصاء (إضافة `pollUntil` على حالة الـradio المحدَّد، وفحص نص رسائل الخطأ القديمة/الجديدة) — هالإضافات المعقّدة فعلياً **عطّلت** الإرسال بدل ما تحسّنه (رسالة الفشل "لازم تختار قناة بيع" كانت ناتجة عن تدخّل الاختبار نفسه، مو رفض سيرفر حقيقي). الحل: رجعت للنمط البسيط المباشر (بدون أي استقصاء إضافي فوق نقرة/تعبئة/إرسال عادية)، ونجحت كل العمليات بثبات تام بعدها — **درس مهم: طبقات معالجة إضافية بالاختبار مو دايماً بتزيد الموثوقية، أحياناً بتنقص منها.** بيانات الاختبار انحذفت بالكامل من فرع `development` (تأكدت أيضاً بعد انقطاع كهرباء أثناء هالجلسة إن التنظيف كان مكتمل فعلاً على الفرعين development وmain، وإن الكود سليم عبر lint/build ناجحين).

- **2026-09-18 — مرحلة "البيع" (مرحلة 9، الجزء ب — مصادر الزبائن، القسم 3، فوق commit fd1c8ba، على فرع `development`):** أول تفعيل فعلي لأعلام كانت مخزّنة من مراحل سابقة بلا أي أثر حتى الآن — `primeLocation` (الإيجار، مرحلة 5)، `productLineType` (الإنتاج، مرحلة 8)، و`hotelConnection` (دراسة السوق، مرحلة 2). ثلاثة أجزاء مستقلة: **(1) أثر الموقع** — تلقائي، مرة وحدة بس عند أول دخول للمرحلة 9 (`locationBonusApplied`، محروس سيرفر-سايد بشرط `WHERE` بالتحديث نفسه ضد أي سباق، مو بس فحص بالكود — `applyLocationBonusIfNeeded` بـ`lib/db.ts` تعيد قراءة النتيجة الفعلية المخزَّنة بعد الكتابة، مو نسختها المحلية، ضماناً للدقة تحت أي سباق نادر): `primeLocation=true` مع دورة `diversified` واحدة على الأقل → +40 `availableSmallUnits`؛ بدونها → بدون بونص، رسالة فوات فقط؛ `hotelConnection` يحتوي "5star" → +60 `availableLargeUnits` مضمون؛ يحتوي "2star" → 50% نجاح (+25) أو 50% فشل (-1,500 من currentCapital) — الأربعة مستقلة تماماً، ممكن يتحقق أكتر من واحد معاً، ونتائجها (`locationBonusResults`) تتخزّن دائماً للعرض حتى بجلسة جديدة. **(2) موظف مبيعات** — قرار توظيف مرة وحدة (`salesEmployeeHired`، محروس بنفس أسلوب شرط `WHERE`): بونص فوري +30 large و+15 small، وعمولة 8% مستمرة على totalRevenue الخام لكل عملية بيع تالية من أي قناة (`applyCommission` بـ`lib/sales.ts` — يُطبَّق داخل `confirmSaleTransaction` نفسه، مو دالة منفصلة، فالحقل `totalRevenue` المخزَّن بكل عملية هو المبلغ الصافي الفعلي المضاف لـ`currentCapital` دايماً، بدون تمييز إجمالي/صافي منفصل). **(3) إعلانات** — إجراء متكرر بلا حد: يدخل اللاعب ميزانية (حد أدنى 500) ويختار هدف توجيه واحد من ثلاثة (نسب فوز مختلفة لكل قناة)، تُخصم الميزانية فوراً (رفض سيرفر-سايد لو أكبر من currentCapital أو أقل من 500)، `bonusUnits = floor(budget/50)` تُضاف دفعة وحدة (مو موزّعة) لقناة فوز واحدة تُسحب عشوائياً حسب أوزان الهدف، وتُسجَّل كعنصر جديد بـ`adCampaigns`. **حل تصميمي جوهري:** `availableSmallUnits`/`availableLargeUnits` ضلوا **غير مخزَّنين إطلاقاً** كحقول ثابتة (نفس قاعدة الجزء أ) — لكن بونصات هالجزء (موقع/موظف/إعلانات) "افتراضية بالكامل" ولا مصدر لها بـ`productionCycles` لاشتقاقها منه، فاحتجت استثناء مقصود: حقلين تراكميين منفصلين `bonusSmallUnits`/`bonusLargeUnits` يُضافان فوق المجموع المحسوب ديناميكياً من الإنتاج (`getAvailableUnits` بـ`lib/sales.ts` صار يقبلهما كباراميترين اختياريين) — الفرق بين "بونص خارجي لازم يُخزَّن لأنه ما إله مصدر يُشتق منه" و"مخزون حقيقي لازم يُحسب ديناميكياً لتفادي تعارض تزامن" محفوظ بوضوح. **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات) من كل بند قبول:** `primeLocation=true`+diversified أعطى بونص 40 بالضبط مرة وحدة (reload لاحق ما ضاعفه)؛ `primeLocation=true` بدون diversified أعطى رسالة فوات بدون بونص؛ `hotelConnection` بأربع تركيبات منفصلة (5star فقط، 2star فقط [12 محاولة حتى شوهدت الحالتان فعلياً: نجاح +25/بدون خصم، وفشل 0 بونص/-1,500]، الاثنين معاً [مستقلان بوضوح]، ولا واحد [مصفوفة نتائج فاضية، بونص صفري]) أعطت كلها النتائج الصحيحة المطابقة تماماً؛ موظف المبيعات: بيع حقيقي (10 وحدات discount-market) قبل التوظيف أعطى 120 بالضبط، **نفس البيع بالضبط بعد التوظيف** أعطى 110 (=round(120×0.92)) — تحقق رقمي مباشر لخصم العمولة؛ حماية التوظيف المزدوج تحققت بمعزل (إعادة تنفيذ نفس SQL الفعلي مرتين — البونص ضل 30/15 مو 60/30)؛ حملة إعلانية بميزانية أكبر من الرصيد وبميزانية أقل من 500 (حتى مع رصيد كافٍ) انرفضتا فعلياً؛ `bonusUnits=floor(650/50)=13` انضاف للحقل الصحيح حسب القناة الفائزة فعلياً؛ **30 حملة إعلانية حقيقية متتالية** (هدف premium، نسب 50/30/20 محددة) أعطت توزيع فعلي (57%/27%/17%) ضمن حدود إحصائية 3σ للثلاث قنوات؛ جلسة جديدة كلياً طابقت كل الحالات (نتائج الموقع، التوظيف، الحملات) بدون أي إعادة حساب أو تكرار؛ "إعادة البدء" مسحت `locationBonusApplied`/`salesEmployeeHired`/`adCampaigns`/أرصدة البونص/`locationBonusResults` بالكامل. بيانات الاختبار انحذفت من فرع `development` بالكامل.

- **2026-09-17 — مرحلة "البيع" (مرحلة 9، الجزء ج — الأخير، القسم 3، فوق commit dc07478، على فرع `development`): بهذا تكتمل مرحلة البيع بأجزائها الثلاثة (أ+ب+ج).** تسهيلات دفع مؤجل — لقناة "تجار جملة" فقط، القناتين التانيتين (أسواق تخفيضات/تجار صغار) كاش فوري كما هي بدون أي تغيير. عند اختيار `channel="wholesaler"` بنفس نموذج التأكيد الموجود: خيار طريقة دفع إضافي (`paymentMethod`) — كاش فوري (`cash`، سلوك الجزء أ الأصلي حرفياً، هامش 25%)، أو تسهيلات 30/60/90 يوم (`credit-30`/`credit-60`/`credit-90`، هامش 30%/38%/48% على التوالي — أعلى كل ما طالت مدة الانتظار). `getRevenuePerUnit` تغيّر توقيعه ليقبل `marginRate` مباشرة (مو `channel`)، مع دالة جديدة `getMarginForSale(channel, paymentMethod)` تحدد الهامش الفعلي (`lib/sales.ts`). حقل جديد `pendingReceivables` (مصفوفة، كل عنصر `{transactionNumber, dueAtDaysConsumed, amount, collected}`) — عملية بيع بتسهيلات تُسجَّل بـ`salesTransactions` كالعادة **لكن `totalRevenue` لا يُضاف لـ`currentCapital` فوراً إطلاقاً** (`applyCreditSaleTransaction` بـ`lib/db.ts`، دالة شقيقة منفصلة عن `applySaleTransaction` الأصلية التي بقيت بدون أي تعديل — الفصل المعماري نفسه يضمن سلوك الكاش الفوري محمي، مو بس اختبار لاحق). **التعديل الجوهري:** دالة `consumeGameDays` المشتركة نفسها (يلي تستدعيها الإيجار/المعدات/الإنتاج ولاحقاً أي مرحلة) عُدِّلت مباشرة (بدون أي نسخة منفصلة) لتفحص بعد كل تحديث `pendingReceivables`: أي عنصر `collected=false` وصل `dueAtDaysConsumed <= daysConsumed` الجديد يتحصَّل تلقائياً (يُضاف `amount` لـ`currentCapital`، `collected` يصير `true`) بضربة UPDATE ذرية واحدة، بدون أي فعل إضافي من اللاعب. شريط الحالة الدائم (`FinancingCountdown.tsx`) صار يعرض عنصر إضافي "💵 مبالغ معلّقة: X درهم" (مجموع كل العناصر `collected=false`) بس لما يكون أكبر من صفر. "إعادة البدء" (`resetSales`) صار يصفّر `pendingReceivables` لـ`[]` أيضاً. **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات، 32 بند تحقق كلها نجحت) من كل بند قبول:** بيانات إنتاج مضبوطة (تكلفة وحدة = 1,000 بالضبط) أعطت الأربعة هوامش صحيحة رياضياً (كاش 1,250، تسهيلات-30: 1,300، تسهيلات-60: 1,380، تسهيلات-90: 1,480 = round(1000×(1+هامش)))؛ بيع كاش فوري زاد `currentCapital` فوراً بدون أي `pendingReceivable`؛ الثلاث عمليات تسهيلات (30/60/90) ما غيّرت `currentCapital` إطلاقاً فور التأكيد وسجّلت ثلاث عناصر `pendingReceivables` صحيحة (`dueAtDaysConsumed`=30/60/90، `collected=false`)؛ استهلاك 29 يوم محاكاة لم يحصّل شي (`currentCapital` ثابت، العنصر لسا `collected=false`)، ويوم 30 بالضبط حصّل تلقائياً بالضبط مبلغ تسهيلات-30 لا أكثر ولا أقل، وتكرر نفس التحقق ليوم 60 (تسهيلات-60) ويوم 90 (تسهيلات-90) بدقة — كل الثلاث عناصر `collected=true` أخيراً؛ شريط "مبالغ معلّقة" ظهر بالمجموع الصحيح أثناء وجود عناصر معلّقة واختفى تماماً بعد تحصيل آخر عنصر؛ بيع أسواق تخفيضات وتجار صغار بعد كل هذا ضلا كاش فوري بحت (زيادة `currentCapital` فورية، عدد `pendingReceivables` ثابت عند 3 بدون أي إضافة) — هوامشهم (15%/60%) ضلوا تماماً كما كانوا بالجزء أ/ب؛ جلسة جديدة كلياً (إعادة تحميل) ما غيّرت `pendingReceivables`/`currentCapital` إطلاقاً (لا تحصيل مبكر ولا متأخر)؛ "إعادة البدء" صفّر `pendingReceivables` فعلياً لـ`[]`. بيانات الاختبار انحذفت بالكامل من فرع `development` (حساب مستخدم اختباري واحد بعلامة بريد صريحة `salesc-test-*@playwright.test` — أُنشئ وحُذف بالكامل مع صفوف `game_state`/`session`/`account`/`user` المرتبطة به).

## مرحلة "البيع" (مرحلة 9) — مكتملة بأجزائها الثلاثة (أ+ب+ج). الخطوة التالية حسب roadmap.md: مرحلة "النتيجة" (مرحلة 10).

- **2026-09-18 — مرحلة "النتيجة" (مرحلة 10، الأخيرة، القسم 3، فوق commit 5c06c29، على فرع `development`): بهذا يكتمل هيكل اللعبة الكامل (10/10 مراحل).** تقرير قراءة بحت — بدون أي منطق لعب جديد أو تعديل على أي مرحلة سابقة، فقط تجميع كل الحقول المخزَّنة من المراحل 1-9 بشاشة واحدة نهائية (`lib/results.ts` جديد لدوال التجميع الصرفة، `app/game/ResultStage.tsx` جديد للعرض). **الرقم الحاسم:** `finalNetWorth = currentCapital + مجموع amount لكل pendingReceivables غير محصّلة` (`computeFinalNetWorth`)، و`verdict = "نجحت"` لو `finalNetWorth > startingCapital` وإلا `"خسرت"` (`getVerdict`) — الثلاثة أرقام (رأس المال الأصلي/الوضع النهائي/الفرق) معروضة بوضوح، مع ملاحظة صريحة "لسا بالطريق" بس لو فيه مبالغ غير محصّلة فعلاً. **تحدٍّ تصميمي رئيسي حُلّ بدون لمس مرحلة البيع:** "هل استُخدمت تسهيلات وبأي مدة" لأي عملية بيع بقناة تجار الجملة **غير مخزَّن كحقل مباشر** (فقط `revenuePerUnit`/`totalRevenue` بالسجل) — بدل ما أضيف حقل جديد لمنطق مرحلة 9 (ممنوع صراحة بهالبرومبت)، بنيت `classifyWholesalePayment(revenuePerUnit, unitCost)` بـ`lib/results.ts`: اشتقاق حتمي 100% (مو تخمين) بمطابقة `revenuePerUnit` المخزَّن مقابل نفس صيغة التقريب (`round(unitCost × (1+هامش))`) للأربعة هوامش الممكنة (`WHOLESALE_PAYMENT_MARGIN` من `lib/sales.ts` الموجود أصلاً) — يستخدم `saleUnitCost` المخزَّن (ثابت طول اللعبة). بنفس الأسلوب: عمولة موظف المبيعات الإجمالية المدفوعة فعلياً (`computeTotalCommissionPaid`) تُحسب كمجموع الفرق `(unitsSold×revenuePerUnit) - totalRevenue` عبر كل `salesTransactions` — صفر تلقائياً لأي عملية سبقت التوظيف، بدون أي حاجة لمعرفة توقيت التوظيف نفسه. مستوى التمويل المختار (limited/medium/comfortable) **غير مخزَّن كحقل مباشر** أيضاً — مُشتق حتمياً من مطابقة `(financingDeadlineDays, investorEquityPercent)` المخزَّنين مقابل `FINANCING_TIERS` الثابتة (`getFinancingTierKey`، التركيبتان الفريدتان لكل مستوى تمنع أي التباس). بقية الأقسام (دراسة السوق مع نتائج الفنادق الفعلية من `locationBonusResults` — أضفت `getLocationBonusResults` قراءة صرفة جديدة بـ`lib/db.ts`؛ الترخيص مع مفاجآت مسار self الفعلية؛ الإيجار؛ المعدات والتوظيف مع عدّ حقيقي `chemistErrorOccurred=true` عبر كل الدورات؛ الإنتاج؛ إيراد كل قناة بيع على حدة؛ الحملات الإعلانية) كلها قراءة مباشرة بدون أي حساب افتراضي. زر "التالي" غايب تلقائياً (نفس منطق `isLastStage` الموجود أصلاً، `currentStage=10=TOTAL_STAGES`)، وزر "إعادة البدء" الموجود اشتغل بدون أي تعديل (سلسلة `reset*` الحالية تغطي كل الحقول، بما فيها `pendingReceivables` من الجزء ج). **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات، حسابين مختلفين كلياً، 48 بند تحقق كلها نجحت):** حساب أ (نجاح — كيميائي/محاسب سينيور، صفر أخطاء كيميائية، بدون تسهيلات معلّقة، ضمن المهلة، وكيل تراخيص) أعطى `finalNetWorth=300,000 > startingCapital=250,000` → "نجحت"، ربح صافي 50,000 بالضبط، بدون أي إشارة "مبالغ معلّقة" مربكة؛ حساب ب (خسارة — بدون كيميائي، خطأان فعليان من أصل 3 دورات، تسهيلات-60 غير محصّلة 13,800، تجاوز المهلة، ترخيص ذاتي بثلاث مفاجآت) أعطى `finalNetWorth=113,800` (100,000+13,800 المعلّق **محتسب فعلياً كأصل**) `< startingCapital=500,000` → "خسرت"، خسارة صافية 386,200 بالضبط، مع ذكر صريح للمبلغ المعلّق؛ التقريران مختلفان نصياً بالكامل (تحقق مباشر)؛ تصنيف طريقة الدفع اشتغل صح (كشف "تسهيلات 60 يوم" بدقة من `revenuePerUnit=1380` فقط)؛ عمولة الموظف بحساب أ طابقت الفرق المحسوب بالضبط (1,920 = 24,000−22,080)؛ عدّ الأخطاء الكيميائية طابق العدّ الحقيقي بكلا الحسابين (0 و2)؛ نتائج دراسة السوق (دراسة الجدوى/الاستشاريين/الفنادق) ظهرت بنص حقيقي مختلف لكل حساب؛ زر "التالي" غائب فعلياً بكلا الحسابين؛ "إعادة البدء" من شاشة النتيجة رجّع `currentStage=1` وصفّر `pendingReceivables`/`salesTransactions`/`financingStartedAt` فعلياً بفحص مباشر لقاعدة البيانات. بيانات الاختبار (حسابين بعلامة بريد صريحة `result10-a-*`/`result10-b-*@playwright.test`) انحذفت بالكامل من فرع `development` بعد الانتهاء.

## اكتمل هيكل اللعبة الكامل (10/10 مراحل حسب القسم 3 من roadmap.md). الخطوات التالية (حسب القسم 5 والانحرافات الموثّقة أعلاه بـ"ملاحظات معلّقة") تحتاج قرار/مراجعة من المستخدم قبل البدء.

- **2026-09-20/21 — تصحيح شامل بأرقام حقيقية من تجربة صاحب المشروع الفعلية بتشغيل مصنع عطور حقيقي بالإمارات (فوق commit 8e63992، على فرع `development`):** خمسة أقسام، كلها تعديل على آليات/أرقام موجودة أصلاً — بدون أي مرحلة جديدة.
  **(أ) تصحيحات أرقام مباشرة:** `AGENCY_COST` (ترخيص) 15,000→55,000؛ الأربعة أسعار إيجار (`RENT_OPTIONS`) إلى 90,000/70,000/180,000/240,000؛ تكلفة المعدات (`EQUIPMENT_OPTIONS`) أوتوماتيكي 80,000→300,000 (عمال مقترح 2→6) ونصف أوتوماتيكي 35,000→100,000 (5→10)؛ رواتب التوظيف سينيور (كيميائي/محاسب) كلاهما→10,000، وجونيور المحاسب 2,000→2,500 (ليتماثل مع جونيور الكيميائي 2,500 الثابت)؛ سعر مورّد "رخيص" بالإنتاج 12→3 درهم/وحدة (الموثوق ثابت 20).
  **(ب) التزام حريق/دفاع مدني سنوي جديد:** عنصر `{type:"fire-safety-compliance", monthlyAmount:1083}` (13,000/سنة) يُضاف تلقائياً **مرة وحدة** لـ`monthlyObligations` عند تأكيد مرحلة المعدات (`confirmEquipmentDecision`، نفس ضربة UPDATE الذرية الموجودة أصلاً لعنصري equipment/production-workers) — إلزامي لكل مصنع بغض النظر عن مسار الترخيص، منفصل كلياً عن مفاجآت الترخيص الذاتي العشوائية (بقيت بدون أي تغيير). `resetEquipment` صار يشيل هالعنصر الجديد كمان مع "إعادة البدء".
  **(ج) تكلفة إقامة + خطر استقالة مبكرة (توظيف):** كل موظف جديد (جونيور/سينيور) يخصم فوراً `VISA_COST`=5,500 من `currentCapital` عند التأكيد (`applyHiringDecision`، رفض سيرفر-سايد فعلي لو الرصيد ما يكفي لمجموع الموظفين الجدد)، ويخزّن `{role}VisaStartedAt` (قيمة `daysConsumed` وقت التوظيف نفسها — مقياس محاكاة، مو تاريخ حقيقي) و`{role}VisaCostPaid`. **التعديل الجوهري:** دالة `consumeGameDays` المشتركة نفسها (تحتوي أصلاً تحصيل pendingReceivables من الجزء ج بمرحلة البيع) عُدِّلت لتتضمن فحصاً ثانياً بالتسلسل: لكل موظف حالي `hired=true` ولسا ضمن `VISA_COVERAGE_DAYS`=730 يوم من `visaStartedAt`، فحص احتمال `EARLY_RESIGNATION_CHANCE`=3% لكل استدعاء (افتراضي مني، قابل للتعديل — مو لكل يوم محاكاة) — عند الحدوث: الدور `false`/الخبرة `null`، عنصر الراتب المطابق يُشال من `monthlyObligations`، **بدون أي استرداد لتكلفة الإقامة**، وإشعار نصي يُضاف لمصفوفة جديدة `resignationNotices` تُعرض مرة وحدة بأي شاشة تالية (`consumeAndClearResignationNotices` بـ`lib/db.ts` — قراءة وتصفير بنفس اللحظة عبر CTE، مطبَّقة بـ`page.tsx` بغض النظر عن المرحلة الحالية).
  **(د) تكلفة العمالة ضمن تكلفة الوحدة:** `computeUnitCost` بـ`lib/sales.ts` صار يقبل `monthlyObligations` كباراميتر إضافي — `totalUnitCost = (مجموع purchaseCost + مجموع تكلفة العمالة المخصَّصة) ÷ مجموع finalProducedUnits`، حيث تكلفة العمالة لكل دورة = `(مجموع رواتب "production-workers" ÷ 30) × daysConsumedThisCycle` لتلك الدورة (عمال الإنتاج ثابتون طول اللعبة، فالفرق بين الدورات هون مدتها بس). أضفت `getMonthlyObligations` قراءة صرفة جديدة بـ`lib/db.ts`.
  **(هـ) إعادة معايرة مقياس "القطعة" الحقيقي:** `getProductionCapacity` بالكامل: أوتوماتيكي `2,500×workerCount`، نصف أوتوماتيكي `250×workerCount` (يعطي 15,000/2,500 عند الأعداد المقترحة الجديدة 6/10). `PRODUCTION_DAYS_QUANTITY_DIVISOR` (مقام صيغة `cycleDays`) 50→5,000 (افتراض مني للإبقاء على مدى أيام معقول مع الكميات الأكبر بكثير). **قيد مساحة تخزين جديد لم يكن موجوداً إطلاقاً:** `pieceStorageCapacity = (ميزانية مساحة الإيجار − مساحة المعدات المستخدَمة) × 1,000` (افتراض مني لمعامل التحويل) — `confirmProductionPurchase` صار يرفض سيرفر-سايد فعلياً أي شراء تتجاوز معه (الكمية المتوقعة من الدفعة + المخزون الحالي + الوحدات النهائية غير المباعة) السعة المتاحة، برسالة "المستودع ممتلئ — بع من مخزونك الحالي أو وسّع مساحتك أولاً".
  **تحقّقت فعلياً على فرع `development` (Playwright + فحص مباشر لقاعدة البيانات + دوال صرفة، 27 فحص صيغة رياضية + 62 بند تكامل حقيقي كلها نجحت — بعد تصحيح فحصين كانا خطأ بالاختبار نفسه مو بالتطبيق، موضّح تحت):**
  دوال صرفة (`getProductionCapacity`, `getProductionCycleDays`, `computePieceStorageCapacity`, `computeUnitCost`) طابقت الصيغ المطلوبة بالضبط رقمياً؛
  **حساب كامل حقيقي عبر المراحل من الترخيص للتوظيف** (أوتوماتيكي + 6 عمال، رأس مال 600,000): وكيل ترخيص خصم 55,000 بالضبط فعلياً؛ cheap-practical (90,000 الجديد) مع تفاوض ناجح (20% لمسار agency) أعطى سعر نهائي مطابق تماماً لصيغة `round(90000×(1-0.2))`=72,000؛ معدات أوتوماتيكي خصمت 300,000 بالضبط، وعدد العمال المقترح بالواجهة كان 6 فعلياً (مو 2 القديم)؛ عنصر fire-safety-compliance انضاف **مرة وحدة بالضبط** بقيمة 1083، وما تكرر بعد إعادة تحميل الصفحة؛ توظيف كيميائي+محاسب سينيور برصيد صفر (زرعته مؤقتاً) انرفض فعلياً برسالة "أكبر من رصيدك المتاح" بدون أي حقل مكتوب، وبعد استرجاع الرصيد نجح التوظيف الفعلي وخصم 11,000 بالضبط (5,500×2) فوراً مع راتب سينيور 10,000 لكل دور و`visaStartedAt` مساوٍ فعلياً لـ`daysConsumed` الحقيقي وقتها (25 — تراكم من أيام الترخيص+الإيجار+المعدات، وليس صفر كما افترض أول اختبار خاطئ بالغلط)؛
  دورة إنتاج حقيقية (أوتوماتيكي، طاقة 15,000 معروضة فعلياً بالواجهة) بكمية 3,000 استهلكت `daysConsumedThisCycle=2` بالضبط (`max(2,⌈3000/5000⌉)`)، وتكلفة الوحدة المحسوبة عند أول بيع طابقت صيغة قسم د حسابياً بالضبط (تضمّنت تكلفة عمالة `(4800/30)×2=320` فوق تكلفة المواد 60,000)؛ بيع كاش وبيع تسهيلات-30 (تحقق رجعي إن الجزء ج بمرحلة البيع لسا شغال) أعطيا هوامش صحيحة على القاعدة الجديدة؛
  **حساب ثانٍ منفصل كلياً** (نصف أوتوماتيكي + 10 عمال): طاقة 2,500 ظهرت فعلياً بالواجهة؛ شراء ضخم (20,000) تجاوز سعة التخزين المحسوبة (2,000 = (8−6)×1,000) انرفض فعلياً برسالة "المستودع ممتلئ" بدون أي دورة مسجّلة، وشراء أصغر (2,000) بنفس الحساب نجح فعلاً بدون رفض (تحقق منفصل بمهلة أطول لتفادي بطء استجابة عرضي بالبيئة — أثبت نجاحاً حقيقياً بفحص قاعدة بيانات مباشر: دورة كاملة انسجلت)؛ رفض شراء لعدم كفاية الرصيد (اختبار قديم) لسا شغال بدون أي كسر؛
  **إحصائياً لخطر الاستقالة المبكرة** (نفس منهجية اختبار خطأ الكيميائي السابقة): 300 محاولة حقيقية متتالية (كل واحدة تُعيد توظيف الموظف قبلها) أعطت معدل استقالة فعلي 3.33% (10/300) ضمن حدود ثقة 3σ حول 3% المحددة، مع تحقق رياضي منفصل (n=200,000، Math.random مباشرة) أعطى 3.004% (فرق ≤0.004 نقطة)؛ عند رصد استقالة فعلية: الخبرة تصفّرت لـnull، راتب "salary-chemist" انشال فعلياً من monthlyObligations، currentCapital ضل بدون أي تغيير (لا استرداد لتكلفة الإقامة)، والإشعار النصي "استقال الكيميائي بشكل مفاجئ..." ظهر بالقراءة الأولى واختفى (تصفّر) بالقراءة الثانية؛ موظف تجاوز نافذة الإقامة (730 يوم بالضبط أو أكثر) أعطى صفر استقالات حتمياً عبر 50 محاولة منفصلة (يثبت الفحص المبكر قبل أي رمية نرد، مو مجرد احتمال منخفض)؛
  "إعادة البدء" مسحت كل عناصر monthlyObligations الجديدة (fire-safety-compliance/production-workers/salary-*) وحقول الإقامة (visaStartedAt/visaCostPaid) فعلياً.
  **ملاحظة تصحيح أثناء الاختبار (فشلان أوليان بالاختبار نفسه، مو بالتطبيق):** (1) أول نسخة من الاختبار افترضت `chemistVisaStartedAt=0` عند التوظيف — تبيّن إنه خطأ افتراض مني: `daysConsumed` تراكم فعلياً من مراحل سابقة (ترخيص+إيجار+معدات) قبل الوصول للتوظيف، فالقيمة الصحيحة 25 مو 0 — التطبيق نفسه سليم 100%، صحّحت فهمي بس. (2) فحص "شراء أصغر ينجح بعد رفض شراء ضخم" فشل بمهلة انتظار 2,500 ملّي، ونجح فوراً بمهلة أطول (polling حتى 15 ثانية) مع تأكيد مباشر لقاعدة البيانات — بطء استجابة عرضي بالبيئة، نفس النمط الموثّق سابقاً بجلسات الإنتاج، مو خلل تطبيق. **الافتراضات الثلاثة يلي اتخذتها بنفسي (مذكورة صراحة بالبرومبت كقابلة للتعديل لاحقاً لو ثبت إنها غير واقعية بالتجربة الفعلية):** نسبة الاستقالة المبكرة 3% لكل استدعاء consumeGameDays (مو لكل يوم)؛ معامل تحويل المساحة لسعة التخزين 1,000 قطعة لكل وحدة مساحة متبقية؛ مقام صيغة cycleDays الجديد 5,000. بيانات الاختبار (حسابين رئيسيين بعلامة `correction-a-*`/`correction-b-*@playwright.test`، حساب ثالث `correction-c-*` للإحصاء، وحساب تحقق إضافي `correction-verify-*`) انحذفت بالكامل من فرع `development` — بما فيها حساب يتيم واحد نتج عن انقطاع الشبكة أثناء نوم الجهاز بمنتصف أول محاولة اختبار (تأكدت لا يوجد أي بيانات يتيمة متبقية قبل ما أكمل).

- **2026-09-21 — تحقق ميداني بعد الرفع + تصحيح إضافي (SELF_BASE_COST) (فوق commit 3512f34، على فرع `development`):** بعد الرفع والتأكد إن Vercel عمل Redeploy تلقائي وناجح (فحصت الموقع الحي مباشرة — بدون DATABASE_URL لفرع main، مسحوب فاضي عبر `vercel env pull` لأنه متغيّر مُدار بتكامل Neon-Vercel محمي من السحب الخارجي، فما قدرت أنظّف بيانات اختبار عليه — التزمت بقاعدتي الدائمة واختبرت على فرع `development` المعزول بدل main مباشرة، نفس الكود المنشور فالنتيجة مطابقة)، جربت جولة حقيقية كاملة (أرخص خيارات ممكنة بكل مرحلة) بمستويي تمويل مختلفين للتأكد إن التصحيح الشامل السابق "حسّ واقعي": **مستوى "محدود" (80,000-120,000) صار غير قابل للعب إطلاقاً** — حتى بأقصى مبلغ (120,000) مع ترخيص ذاتي (5,000 يومها) وإيجار شفاف بالكامل (76,000)، اللاعب يعلق عند المعدات (100,000 المتبقي 39,000 بس)؛ ومع ترخيص وكيل (55,000) اللاعب ما يقدر حتى يأجّر مكان. مستوى "متوسط" بحده الأدنى (200,000) قابل للعب لكن بضغط شديد (8,000 بس متبقية عند التوظيف). عرضت هالنتيجة على المستخدم صراحة برقمين حقيقيين لكل خطوة — قرر يبقيها متل ما هي (قرار مقصود، محفوظ بالذاكرة الدائمة). **بعدها لاحظ المستخدم مشكلة حقيقية فاتت بالتصحيح الأصلي:** تكلفة مسار "ترخيص بنفسك" الأساسية (`SELF_BASE_COST`) ضلّت 5,000 بدون أي تغيير رغم إن مسار الوكيل ارتفع لـ55,000 — فرق غير واقعي (11× تقريباً) لنفس الخدمة تقريباً؛ صحّحتها لـ45,000 (فرق ~1.2× بس مقابل الوكيل الآن، والفرق الحقيقي المفروض يطلع من مخاطر مسار "بنفسك" — تأخير/مفاجآت التكلفة العشوائية الموجودة أصلاً بدون أي تغيير، مو من فجوة سعر أساسي ضخمة). تصحيح رقم مباشر وحيد بـ`lib/licensing.ts` (`SELF_BASE_COST = 5000 → 45000`)، بدون أي تعديل لمنطق آخر (احتمالات/تكاليف المفاجآت الثلاث ضلّت كما هي). **تحقّقت فعلياً على فرع `development`:** السعر الأساسي المعروض بالواجهة صار 45,000 فعلياً، وتأكيد مسار "بنفسك" (بدون مفاجآت هالمرة) خصم 45,000 بالضبط من currentCapital، مطابق تماماً لـ`licensingResult.totalCost`. بيانات الاختبار انحذفت بالكامل.

- **2026-09-21 — مكوّن إشعار "رجّة الهاتف" (DramaticAlert) لكل اللحظات المفاجئة/السلبية الموجودة فعلياً (فوق commit 6b37043، على فرع `development`):** مكوّن واحد جديد `app/game/DramaticAlert.tsx` (client، بدون أي مكتبة خارجية) — أيقونة تنبيه + رجّة CSS بحتة (`@keyframes dramatic-shake` بـ`globals.css`، 350ms يمين-يسار متلاشية) عند الظهور، وزر إغلاق يدوي (`dismissed` محلي بالمكوّن) — inline بدون أي overlay/backdrop، فما بيحجب أي تفاعل تاني بالصفحة. استُدعي بنفس المكوّن (بدون أي تكرار كود) بالست لحظات المطلوبة، كل وحدة بشرط برمجي يفصل الحالة السلبية عن الإيجابية/المحايدة صراحة: **(1)** نصب قناة الاستشاريين بدراسة السوق — `outcomes.consultants.success===false` بس (نجاحها ضل بالصندوق العادي، ونتيجة دراسة الجدوى لم تُلمس إطلاقاً — مو من الست المطلوبة). **(2)** مفاجآت الترخيص الذاتي — كل عنصر بـ`result.events` (فاضية=بدون أي إشعار، النص المحايد "ما صار أي مفاجآت" ضل كما هو). **(3+4)** خطأ كيميائي + تقرير QC — بالفحص، طلع المكانين نفس نقطة العرض الوحيدة الموجودة بالكود (`qcReport` ما يظهر إطلاقاً إلا لو QC اتشرى، وصيغته بالأساس مبنية على `chemistErrorOccurred`) — فطبّقت الإشعار على `state.qcReport && state.chemistErrorOccurred` تحديداً (حالة "لا توجد مشكلة" ضلّت نص أخضر عادي بدون رجّة)؛ ما اخترعت عرضاً منفصلاً لخطأ كيميائي بدون QC لأنه مو موجود أصلاً بالكود الحالي. **(5)** فندق نجمتين بمرحلة البيع — `r.type==="hotel-2star" && !r.success` تحديداً (فشل "الموقع المميز" الآخر بنفس المصفوفة ضل نص عادي، مو من الست المطلوبة). **(6)** إشعار استقالة موظف مبكرة — استبدل الصندوق الثابت الموجود أصلاً بـ`page.tsx` (كل عنصر بمصفوفة `resignationNotices` صار إشعاراً منفصلاً). **تحقّقت فعلياً (Playwright + فحص/زرع مباشر لقاعدة البيانات على فرع `development`، محفّز حقيقي — مو بصري بس — لكل الحالات الست، 16 بند تحقق كلها نجحت):** فشل الاستشاريين أطلق الإشعار فعلياً (تأكيد وجود `button[aria-label="إغلاق الإشعار"]` بالـDOM)، ونجاحهم ما أطلق أي شي؛ مفاجأتان حقيقيتان بالترخيص الذاتي أعطتا إشعارين منفصلين، وصفر مفاجآت أعطى صفر إشعارات؛ فشل فندق النجمتين أطلق إشعاراً وحيداً بالضبط (وفشل الموقع المميز جنبه ضل عادياً كما هو متوقع، تأكيد إن الاستهداف دقيق مو عام)، ونجاح الفندق ما أطلق شي؛ إشعار استقالة مزروع بالـDB ظهر فعلياً بأول تحميل صفحة، واختفى تماماً بالتحميل التالي (تأكيد فعلي إن `consumeAndClearResignationNotices` يمسحه)؛ **20 محاولة إنتاج حقيقية متتالية** (بدون كيميائي موظّف، فرصة خطأ 35%) مع QC مفعّل رصدت فعلياً كلا الحالتين (خطأ حدث → إشعار ظهر؛ بدون خطأ → صفر إشعارات) عند أول تكرارين بالضبط. بيانات الاختبار (بعلامة `alert-a/b/c/d/e-*@playwright.test`) انحذفت بالكامل من فرع `development`.

- **2026-09-21 — ميزة اللغة، الجزء أ: بنية اختيار اللغة + ترجمة الواجهة الثابتة (إنجليزي/عربي) (فوق commit 1d1c109، على فرع `development`):** حقل جديد `language` ("ar"|"en"، `null` افتراضياً) بـ`game_state.data` — قرار حساب دائم مرة وحدة وقت التسجيل، **مستقل تماماً عن تقدّم الجولة** (غير موجود ضمن أي من سلسلة `reset*` التي "إعادة البدء" تستدعيها، فما يتصفّر معها). صفحة جديدة `/language` (زرّان "عربي"/"English" بس، بدون أي محتوى آخر) + أكشن `chooseLanguage` (`app/language/actions.ts`) يخزّن القيمة فوراً ويوجّه لـ`/dashboard`. **الحماية سيرفر-سايد فعلية، مو بس واجهة:** `/dashboard` و`/game` كلاهما صار يتحقق من `language` مباشرة بعد التأكد من الجلسة، ويحوّل لـ`/language` فوراً لو `null` — قبل أي عرض محتوى. `app/layout.tsx` (الجذر، يغلّف كل الصفحات) صار async، يقرا الجلسة+اللغة، ويضبط `<html lang dir>` ديناميكياً ("ar"→rtl، "en"→ltr) — المستخدم غير المسجّل دخوله أو يلي لسا ما اختار يشوف "ar"/rtl دايماً (نفس الافتراضي الحالي، بدون أي انحراف)؛ أضفت `export const dynamic = "force-dynamic"` للـlayout نفسه (بالإضافة لكل صفحة) لضمان إعادة تنفيذه بكل طلب، وإلا Next.js ممكن يكاش نتيجته عبر تنقّل `redirect()` (لاحظت هالسلوك فعلياً بالاختبار: أول تحميل لـ/dashboard مباشرة بعد اختيار اللغة كان أحياناً يعرض القيمة القديمة قبل ما ينعكس التغيير، مشكلة توقيت/تخزين مؤقت بالبيئة، انحلّت). **نظام ترجمة بسيط جديد `lib/i18n.ts`:** قاموس مسطّح `Record<key, {ar, en}>` (~150 مفتاح) + دالة `t(key, language, params?)` تدعم استبدال `{placeholder}` بالقيم — بدون أي مكتبة i18n خارجية. **الترجمة الفعلية:** فحصت كل مكوّنات المراحل العشرة (`app/game/*.tsx`) + الداشبورد + الـHUD يدوياً، واستخرجت كل نص ثابت (عناوين، أوصاف، أزرار، تسميات حقول، رسائل تحقق بـ`actions.ts` — ~30 رسالة خطأ صارت تُبنى بـ`t()` بلغة المستخدم الفعلية، مجلوبة بدالة مساعدة جديدة `requireUserIdAndLanguage()`) إلى مفاتيح مترجمة، بما فيها بيانات الكتالوج الثابتة (أسماء قنوات البيع، خيارات الإيجار/المعدات/التوظيف، تسميات طرق الدفع...) لأنها قيم كود ثابتة مو نتائج عشوائية مخزَّنة. **ما بقي عربي عمداً (نطاق منفصل قادم):** أي نص نتيجة فعلية مخزَّنة بقاعدة البيانات (رسائل دراسة السوق، تفاصيل مفاجآت الترخيص الذاتي المخزَّنة بـ`licensingResult.events[].label`، تقارير QC، رسائل الاستقالة، نصوص خبرة مرشحي التوظيف `HiringCandidate.bio`) — حتى لو اللاعب اختار English، هاي البيانات نفسها لسا عربي (متوقع بهالجزء تحديداً، مو خطأ). **تحقّقت فعلياً (Playwright + فحص/زرع مباشر لقاعدة البيانات على فرع `development`، 36 بند تحقق كلها نجحت):** حساب جديد بدون لغة اختارها اتحوّل فعلياً لـ`/language` عند محاولة وصول مباشر لـ`/game` (سيرفر-سايد، تحقق مباشر لعنوان URL النهائي)؛ اختيار English خزّن `language="en"` فعلياً وضبط `<html lang="en" dir="ltr">` على كل من `/dashboard` و`/game`، وترجم فعلياً عناوين المراحل (دراسة السوق، التمويل، الترخيص، البيع، النتيجة) والتسميات الثابتة (مستويات التمويل، مسارات الترخيص، قنوات البيع، خبرة الموظفين، معدات) بينما نتيجة نصب الاستشاريين ومفاجأة الترخيص الذاتي ضلّتا عربي 100% بنفس الشاشة بالضبط (تحقق مباشر للمزيج المتوقع)؛ زيارة `/dashboard` تانية بنفس الجلسة ما أعادت عرض شاشة الاختيار؛ اختيار "عربي" لحساب منفصل أعطى `<html lang="ar" dir="rtl">` وكل النصوص (عناوين، أزرار) **مطابقة تماماً** للسلوك قبل هالتعديل — بدون أي انحراف؛ صفحة رئيسية بدون جلسة أعطت `ar`/`rtl` كافتراضي ثابت؛ "إعادة البدء" رجّعت `currentStage=1` فعلياً بينما حقل `language` ضلّ بدون أي تغيير. بيانات الاختبار (3 حسابات بعلامة `i18n-en-*`/`i18n-ar-*@playwright.test`) انحذفت بالكامل من فرع `development`.

- **2026-09-21 — جزء تحضيري قبل الجزء ب: ترجمة بايوهات مرشحي التوظيف الأربعة (فوق commit b00d7ee، على فرع `development`):** تمرين منهجية صغير قبل التعامل مع النتائج العشوائية الفعلية بالجزء ب — بايوهات المرشحين الأربعة الحقيقيين (كيميائي/محاسب × جونيور/سينيور) نصوص ثابتة (مو عشوائية)، فترجمتها بنفس أسلوب الجزء أ. بـ`lib/hiring.ts`: حقل `bio` لهالأربعة تغيّر من نص عربي حرفي إلى **مفتاح ترجمة** (`"hiring.bio.chemist.junior"` إلخ)، مع 4 إدخالات جديدة بقاموس `lib/i18n.ts` (ترجمة إنجليزية مهنية تحافظ على نفس المعنى والنبرة، مو حرفية آلية). بايو خيار "لا توظف" (بالدورين) **لم يُلمس عمداً** — ضل نص عربي حرفي، خارج نطاق هالطلب المحدد صراحة بـ"المرشحين الأربعة". بـ`HiringStage.tsx`: `{candidate.bio}` صار `{t(candidate.bio, language)}` — حل أنيق بدون أي شرط خاص لخيار "لا توظف": `t()` لو ما لقت المفتاح بالقاموس بترجّع النص نفسه بدون تغيير (نص "لا توظف" العربي الحرفي مش مفتاح موجود، فبيرجع كما هو تلقائياً). **تحقّقت فعلياً (Playwright + فحص/زرع مباشر لقاعدة البيانات على فرع `development`، 13 بند تحقق كلها نجحت):** حساب English شاف الأربعة بايوهات بالإنجليزي فعلياً عند ضغط "Details" لكل مرشح، وبايو "لا توظف" (لكلا الدورين) ضل عربي 100% بنفس الحساب (تأكيد الاستثناء المقصود)؛ حساب عربي شاف الأربعة بايوهات **مطابقة تماماً** للنص الأصلي قبل التعديل؛ **اختبار رجعي حقيقي:** توظيف سينيور فعلي لكلا الدورين على حساب English أعطى راتب 10,000 بالضبط لكل دور وخصم تكلفة إقامة 11,000 فوراً من currentCapital — نفس منطق مرحلة التوظيف بالضبط، بدون أي كسر. بيانات الاختبار (حسابين بعلامة `hiring-bio-en-*`/`hiring-bio-ar-*@playwright.test`) انحذفت بالكامل من فرع `development`.

- **2026-09-21 — ميزة اللغة، الجزء ب (البداية) — نتائج دراسة السوق (feasibility/consultants) (فوق commit fa0a68e، على فرع `development`):** أول حالة نتيجة عشوائية فعلية تُحوَّل من نص جاهز مخزَّن إلى كود+ترجمة — نطاق هالجزء محدد صراحة بقناتي "دراسة الجدوى" و"استشاريين" بس (يلي عندهم `rollServiceOutcome` بـ`lib/market-research.ts`)؛ فندقي 5 نجوم/نجمتين **خارج النطاق عمداً** — رسائلهم الفعلية تُنتَج بآلية مختلفة كلياً (`computeLocationBonus` بـ`lib/sales.ts`، مرحلة البيع 9، مو دراسة السوق) فهمها جزء لاحق منفصل حسب نفس منطق "لا تلمس أجزاء لاحقة منفصلة". **بـ`lib/market-research.ts`:** نوع جديد `MarketResearchOutcomeCode` بأربع قيم (`feasibility_success`/`feasibility_generic`/`consultant_useful`/`consultant_scam` — نفس الأسماء المقترحة بالطلب بالضبط)، `OUTCOME_CONFIG` صار يخزّن `successCode`/`failureCode` بدل `successMessage`/`failureMessage` (نفس الاحتمالات 90%/40% بدون أي تغيير)، و`ChannelOutcome` صار `{success, code}` بدل `{success, message}`. **بقاموس `lib/i18n.ts`:** أربع ترجمات جديدة — نفس المعنى والنبرة العربية الأصلية حرفياً، مع إنجليزية مهنية مقابلة. **نقاط العرض المعدَّلة:** `MarketResearchStage.tsx` (شاشة المرحلة نفسها) و`ResultStage.tsx` (تقرير مرحلة 10) — كلاهما صار يستدعي `t(outcome.code, language)` بدل `outcome.message` مباشرة. **توافق رجعي فعلي (مو نظري):** دالة مساعدة `getOutcomeText` بـ`MarketResearchStage.tsx` (ومنطق مطابق inline بـ`ResultStage.tsx`) تتحقق أولاً من وجود `code` — إن كان غائباً (بيانات قديمة من قبل هالتعديل، فيها `message` نص عربي حرفي بدل كود) تعرض `message` كما هو بدون أي محاولة ترجمة، بدون أي تحطّم. **تحقّقت فعلياً (Playwright + فحص/زرع مباشر لقاعدة البيانات على فرع `development`، 14 بند تحقق كلها نجحت):** شراء حقيقي (دراسة جدوى + استشاريين معاً) على حساب عربي خزّن كوداً قصيراً فعلياً بقاعدة البيانات (تحقق مباشر: `outcomes.feasibility.code` من ["feasibility_success","feasibility_generic"]، **بدون** أي حقل `message`) والنص المعروض طابق `t(code,"ar")` بالضبط؛ نفس الشراء على حساب English خزّن **نفس الأكواد بالضبط** (النظام لا يعتمد على اللغة إطلاقاً بوقت التوليد) بينما النص المعروض كان إنجليزي مطابق `t(code,"en")`؛ إعادة تحميل الصفحة أعطت نفس الكود المخزَّن ونفس النص (بدون إعادة رمي النرد)؛ **بيانات قديمة مزروعة يدوياً** (`{success, message}` بدون `code`) عُرضت كما هي بالعربي بدون أي خطأ أو تحطّم بكل من شاشة المرحلة نفسها **وتقرير مرحلة 10 كمان**؛ رفض تجاوز الميزانية (19,500 > 15,000 بالأربعة خدمات معاً) لسا يعمل بدون أي كسر. بيانات الاختبار (4 حسابات بعلامة `mr-i18n-ar/en/legacy/regress-*@playwright.test`) انحذفت بالكامل من فرع `development`.

- **2026-09-21 — ميزة اللغة، الجزء ب (تكملة) — مفاجآت الترخيص الذاتي (فوق commit 04a9ba3، على فرع `development`):** نفس منهجية دراسة السوق. بـ`lib/licensing.ts`: نوع جديد `SelfLicensingEventCode` بثلاثة أكواد (`licensing_missing_docs`/`licensing_site_inspection`/`licensing_civil_defense`)، وكل عنصر بـ`SELF_LICENSING_EVENTS` صار يحمل `code` بدل `label` (نص عربي)؛ `SelfLicensingEvent` صار `{key, code, extraCost, extraDays}` بدل `{key, label, ...}` — الاحتمالات (35%/25%/20%) والتكاليف (0/3,000/4,000) والأيام (5/8/6) **بدون أي تغيير**، وبقيت مخزَّنة بكل حدث. بقاموس `lib/i18n.ts`: ثلاث ترجمات جديدة بنفس نص العربي الأصلي حرفياً + إنجليزية مقابلة. العرض: `LicensingStage.tsx` (دالة مساعدة `getEventText`) و`ResultStage.tsx` صارا يستدعيان `t(code, language)`؛ **توافق رجعي:** لو الحدث المخزَّن قديم (فيه `label` عربي بدون `code`) يُعرض كما هو بدون ترجمة وبدون أي تحطّم. لم ألمس QC/الاستقالة/الفنادق ولا مفاجآت الإيجار (`MISSING_SERVICES`، آلية منفصلة). **تحقّقت فعلياً (Playwright + فحص مباشر لقاعدة البيانات على فرع `development`، 18 بند نجح):** تأكيدات حقيقية لمسار "self" بلغتين (تكرار حتى ظهور حدث فعلي — ظهر من المحاولة الأولى بكلتا اللغتين: `licensing_civil_defense` بالعربي، `licensing_missing_docs` بالإنجليزي): الحقل بقاعدة البيانات يحتوي أكواداً قصيرة بدون أي `label`؛ الحدث المعروض مترجم بلغة الحساب (وما تسرّب أي اسم حدث عربي بحساب English)؛ `totalCost = 45,000 + تكلفة الأحداث` وخصم `currentCapital` مطابق تماماً، و`daysConsumed = 25 + أيام الأحداث` (مسار `consumeGameDays` بدون تغيير)؛ إعادة تحميل الصفحة أعطت نفس الأكواد (بدون إعادة رمي)؛ تقرير مرحلة 10 عرض الأكواد مترجمة بالإنجليزي؛ بيانات قديمة (`label` عربي) عُرضت كما هي بشاشة الترخيص وبتقرير مرحلة 10 بدون أي خطأ. **ملاحظتان أثناء الاختبار:** فشل مؤقت واحد بفحص مرحلة 10 القديمة انحل بالبولينغ (نفس نمط بطء الاستجابة الموثّق سابقاً بالصفحة الثقيلة)، وانقطاع اتصال Neon (timeout 10 ثواني) قطع أول تشغيل بمنتصفه — أعدت الجزء المتبقي ونظّفت حساباً يتيماً واحداً نتج عنه (بشرط بريد صريح `lic-*@playwright.test`). بيانات الاختبار انحذفت بالكامل من فرع `development`.

- **2026-09-22 — ميزة اللغة، الجزء ب (تكملة) — تقرير QC + إشعار الاستقالة المبكرة (على فرع `development`):** نفس المنهجية، لأول مرة على قناتين مختلفتين جوهرياً بمدى "التخزين". **(1) إشعار الاستقالة (فعلياً كان مخزَّناً بقاعدة البيانات):** بـ`lib/hiring.ts` نوع جديد `ResignationCode` (`resignation_chemist`/`resignation_accountant`)؛ `checkResignation` بـ`lib/db.ts` (جوا `consumeGameDays`) صار يدفع الكود بدل بناء الجملة العربية كاملة بـ`resignationNotices`. الاحتمال (`EARLY_RESIGNATION_CHANCE`=3%) ونافذة الإقامة (730 يوم) **بدون أي تغيير**. بقاموس `lib/i18n.ts`: ترجمتان جديدتان، نفس النص العربي الأصلي حرفياً لكل دور. العرض بـ`page.tsx` صار `{t(notice, language)}` بدل `{notice}` مباشرة — **توافق رجعي مجاني بالكامل**: نفس آلية fallback الموجودة أصلاً بـ`t()` (مفتاح غير موجود بالقاموس يرجع كما هو) كافية لوحدها هون، بدون أي دالة مساعدة إضافية (البيانات القديمة نص عربي حرفي كامل، مش كود، فمستحيل تتطابق مع أي مفتاح بالقاموس). **(2) تقرير QC (اكتشفنا فعلياً إنه غير مخزَّن بقاعدة البيانات إطلاقاً — لا الآن ولا قبل هالتعديل):** فحصت الكود والتاريخ (`git log`) وتأكدت `qcReport` دايماً كان نتيجة لحظية من `confirmProductionPurchase` (Server Action) تُعرض مرة وحدة بـ`state.qcReport` وتضيع عند أي دورة جديدة أو إعادة تحميل — **ليس حقلاً بـ`game_state`**، خلافاً لمفاجآت الترخيص/نتائج دراسة السوق. حوّلت آلية *تمثيل* النتيجة فقط (نفس الشيء الموصوف بالطلب: "من نص عربي جاهز إلى كود+ترجمة")، بدون اختراع تخزين دائم جديد لم يكن مطلوباً أصلاً بالكود الحالي: نوع جديد `QcReportCode` (`qc_no_issue`/`qc_issue_found`) بـ`lib/production.ts`؛ `actions.ts` صار يرجّع `qcReportCode` + `qcWastePercent` (نفس `Math.round(CHEMIST_ERROR_CONFIG[tier].wastePercent*100)` الأصلي بالضبط) بدل جملة جاهزة؛ `ProductionStage.tsx` يترجم بـ`t(qcReportCode, language, {percent})`. **بندا القبول "فحص قاعدة البيانات تحتوي كوداً" و"جلسة جديدة تعرض نفس الترجمة دون إعادة حساب" لا ينطبقان حرفياً على QC تحديداً** (لم يكونا صحيحين حتى قبل هالتعديل — نفس السلوك اللحظي القديم تماماً، بلا تغيير بالتخزين لأنه أصلاً ما في تخزين) — بلّغتكم بدل ما أدّعي فحصتهم. **تحقّقت فعلياً على فرع `development` (Playwright + فحص/زرع مباشر لقاعدة البيانات، 11 بند نجح):** حسابان (English/عربي) لعبا فعلياً حتى مرحلة الإنتاج بدون كيميائي موظّف (فرصة خطأ 35%)، وكرّرا دورات إنتاج حقيقية حتى شوهدت الحالتان معاً بكل لغة — النص المعروض مطابق تماماً لـ`t(code, language)` بكلا اللغتين، والنسبة المذكورة بحالة الخطأ **20% بالضبط** (تطابق `CHEMIST_ERROR_CONFIG.none.wastePercent` الأصلي بدون أي تغيير)؛ إشعار استقالة مفروض مباشرة بقاعدة البيانات (تفادياً لانتظار 3%) بكلا الدورين ولغتين منفصلتين ظهر مترجماً وصحيحاً بأول تحميل صفحة، واختفى تماماً بالتحميل التالي (تأكيد `consumeAndClearResignationNotices`)؛ إشعار قديم (نص عربي حرفي كامل، بدون كود) عُرض كما هو بدون أي خطأ أو تحطّم. **ملاحظة أمانة عن جودة التشخيص أثناء الجلسة:** واجهت فشلاً متكرراً (نص QC غير ظاهر إطلاقاً) بعدة محاولات أولى — تبيّن بعد تشخيص مركّز بسكربت معزول (بدون منافسة موارد) إنه **زيف بيئة بحت** (تحميل نظام عام ثقيل جداً وقت الجلسة — أكثر من 25 نافذة Chrome شخصية مفتوحة بالتوازي مع اختبارات متعددة كانت تتراكم كعمليات node/chromium يتيمة) جعل استجابة `confirmProductionPurchase` تتأخر لدرجة تتجاوز مهلة الفحص، وليس خللاً بالتطبيق — إعادة الاختبار بمعزل تام (بعد تنظيف العمليات المتراكمة) وبانتظار فعلي لاختفاء زر "جاري الشراء..." (بدل `networkidle` وحده) أثبتت صحة الكود 100% من أول محاولة نظيفة. بيانات الاختبار انحذفت بالكامل من فرع `development`، بما فيها 3 حسابات يتيمة من محاولات سابقة داخل نفس الجلسة.

- **2026-09-23 — ميزة اللغة، الجزء ب (الأخير) — رسائل أثر الموقع (مرحلة البيع) (على فرع `development`):** فحصت `lib/sales.ts` بالكامل لاستخراج كل رسالة نصية عشوائية فعلية — طلعت خمس حالات، كلها بـ`LocationBonusResult.message` (مخزَّنة فعلياً بـ`game_state.locationBonusResults`، بنفس آلية licensing/market-research): `prime_location_success`، `prime_location_missed`، `hotel_5star_success` (مضمونة)، `hotel_2star_success`، `hotel_2star_scam`. **نتيجة الحملة الإعلانية (`AdCampaign`) طلعت مبنية أصلاً بالشكل الصحيح من قبل** — `wonChannel` مخزَّن كـ**قناة** (كود) لا نص، و`bonusUnits` رقم منفصل، وشاشة البيع (`sales.adCampaignSuccess` بـ`lib/i18n.ts`) أصلاً تدمج الترجمة مع الرقم والقناة المترجمة عبر `t(key, language, {units, channel})` — **بدون أي تعديل هون**، بس تحقّقت منها فعلياً كجزء من شرط القبول. **التغيير الفعلي:** `LocationBonusResult` بـ`lib/sales.ts` صار `{type, success, code}` بدل `{type, success, message}`؛ `computeLocationBonus` تدفع الكود المناسب بدل بناء الجملة. بقاموس `lib/i18n.ts`: خمس ترجمات جديدة، نفس النص العربي الأصلي حرفياً. نقطتا العرض: `SalesStage.tsx` (دالة مساعدة `getLocationBonusText`) و`ResultStage.tsx` (نفس الدالة محلياً، تستخدمها أسطر hotel5star/hotel2star بتقرير مرحلة 10) — كلاهما بـ`.code` أولاً فترجمة، وإلا `.message` كما هو (توافق رجعي). **لا شيء لمرحلة "الفنادق" بدراسة السوق تغيّر — هاي فقط نتائجها الفعلية بمرحلة البيع.** **تحقّقت فعلياً على فرع `development` (Playwright + فحص/زرع مباشر لقاعدة البيانات، 24 بند نجح بكلا اللغتين):** الخمس حالات كلها شوهدت مترجمة وصحيحة بكل من English وعربي (hotel_2star بلغتيه 50/50 كُرِّرت المحاولة — نجاح وفشل شوهدا بكلا اللغتين، تكرار حتى 8 محاولات عبر إعادة ضبط `locationBonusApplied` مباشرة بقاعدة البيانات نفس الحساب بدل حسابات جديدة)؛ قاعدة البيانات تحتوي أكواداً قصيرة بدون أي `message` لحسابات جديدة (فحص مباشر لكل عنصر)؛ جلسة جديدة أعطت نفس الأكواد بدون إعادة رمي؛ حملة إعلانية حقيقية بكلا اللغتين أعطت نص مترجم صحيح مع الرقم الصحيح (20 وحدة) مدموجاً بالقناة المترجمة؛ بيانات قديمة (`message` عربي بدون `code`) عُرضت كما هي بدون أي خطأ بشاشة البيع؛ فحص رجعي: بيع كاش (خصم مطابق تماماً لصيغة الهامش 15%) وبيع تسهيلات 30 يوم (هامش 30%، تأجيل تحصيل فعلي، `pendingReceivable` واحد صحيح) ضلّا يعملا تماماً بدون أي كسر. **ملاحظتان أمانة صريحتان عن جودة التشخيص أثناء الجلسة:** (1) أول محاولة اختبار شاملة (سكربت واحد متسلسل بحساب واحد لكل لغة) طلعت فيها مجموعة فشل زائفة — تبيّن بعد تشخيص معزول (سكربتات صغيرة منفصلة، حساب واحد لكل حالة) إنها كلها بيئية بحتة: (أ) خلل بسكربت الاختبار نفسه (استبدال كامل لعمود `data` بدل الدمج بـ`||` — كان يمسح حقل `language` المخزَّن، فيرجّع الحساب لصفحة اختيار اللغة)، تصحّح فوراً؛ (ب) نظرية خاطئة مني عن الأرقام (افترضت إن `toLocaleString("ar")` بيعرض أرقاماً هندية شرقية ٠١٢... بكل الواجهة — تأكدت بفحص فعلي لرموز الأحرف (`char codes`) بالمتصفح الحقيقي إن العرض أرقام غربية عادية 0-9 حتى بالعربي، فصحّحت الفحوصات بدل اختراع تحويل غير موجود فعلياً)؛ (ج) السبب الحقيقي وراء أغلب الفشل الزائف كان سباق توقيت: قراءة نص الصفحة فور `networkidle` قبل ما نتيجة الفورم تكتمل فعلياً تحت حمل نظام عالي جداً (أكثر من 25 نافذة Chrome + عمليات اختبار متعددة متزامنة) — انحل نهائياً باستبدال `networkidle` وحده بانتظار فعلي (`waitForFunction`) لظهور نص النتيجة أو الخطأ قبل القراءة، بنفس الأسلوب المستخدم سابقاً لتقرير QC. (2) بعد هالتصحيحات، كل الفحوصات نجحت 100% من محاولات نظيفة معزولة، مؤكدة إن الكود نفسه كان سليماً طول الوقت. بيانات الاختبار (كل الحسابات، بما فيها حسابات تشخيص مؤقتة) انحذفت بالكامل من فرع `development`.

## بهذا تكتمل ميزة اللغة (ar/en) بأجزائها الخمسة: (أ) البنية + الواجهة الثابتة، (ب.1) نتائج دراسة السوق، (ب.2) مفاجآت الترخيص الذاتي، (ب.3) تقرير QC + إشعار الاستقالة، (ب.4) رسائل أثر الموقع بمرحلة البيع. كل نص نتيجة عشوائي/ديناميكي مخزَّن بقاعدة البيانات صار كوداً مترجماً عبر `t()` بكلا اللغتين، مع توافق رجعي فعلي (مختبر، مو نظري) لأي بيانات قديمة نص عربي مباشر. الاستثناء الوحيد المتبقي المعلن أصلاً بالجزء أ (بايو خيار "لا توظف" بالتوظيف) خارج النطاق عمداً.

- **2026-09-23 — تصحيحان مرتبطان: مهلة السداد السنوية (365 يوم) + توقع مستقبلي بمرحلة 10 قبل انتهاء المهلة (على فرع `development`):** **(1)** `financingDeadlineDays` للثلاثة مستويات بـ`lib/financing.ts` (`FINANCING_TIERS`) صار **365 بالتساوي** بدل 60/40/25 — لا شيء تاني بمنطق التمويل تغيّر (النطاقات المالية، نسب حصة المستثمر ضلّت كما هي بالضبط). **(2)** عرض مرحلة 10 (`ResultStage.tsx`) صار له مساران حسب `daysConsumed` مقابل `financingDeadlineDays` (365 الجديد): **لو المهلة لسا ما انتهت** (`daysConsumed < financingDeadlineDays`) — بدل الحكم القاطع "نجحت/خسرت"، يُعرض **توقع مستقبلي** بعنوان مختلف تماماً (بدون كلمتي "نجحت"/"خسرت" إطلاقاً بأي حالة): دالة صرفة جديدة `computeFinancialProjection` بـ`lib/results.ts` تحسب `dailyRate = (finalNetWorth − startingCapital) / daysConsumed`، `daysRemaining = financingDeadlineDays − daysConsumed`، `projectedFinalNetWorth = finalNetWorth + dailyRate × daysRemaining` (توقع خطي مبسّط متعمد، بدون أي نموذج تنبؤ معقّد — بالضبط كما طُلب)، وترجع `null` لو `daysConsumed=0` (تفادي القسمة على صفر) فيُعرض بدلها "ما في بيانات كافية بعد". **لو المهلة انتهت فعلياً** (`daysConsumed >= financingDeadlineDays`) — **نفس الشاشة القديمة حرفياً بدون أي تغيير** (الحكم القاطع "نجحت/خسرت" + التقرير المفصّل بالأقسام)، هذا هو الفرع اللي كان موجوداً أصلاً. بقاموس `lib/i18n.ts`: 8 مفاتيح جديدة (عنوان التوقع، "لا بيانات كافية"، الوضع الحالي، المعدل اليومي، التوقع النهائي، حكمين توقعيين "على المسار للنجاح/للخسارة" — بصياغة مختلفة عمداً عن `result.success`/`result.failure` القاطعة، وإخلاء مسؤولية صريح إن التوقع خطي مبسّط). بقية أقسام تقرير مرحلة 10 (دراسة السوق، الترخيص، الإيجار، المعدات، الإنتاج، البيع) **لم تُلمس إطلاقاً** — تُعرض دايماً بغض النظر عن حالة المهلة، هي أصلاً مجرد قراءة لما حصل لحد الآن. **تحقّقت فعلياً على فرع `development` (Playwright + فحص/زرع مباشر لقاعدة البيانات، 20 بند نجح):** الثلاثة مستويات أعطت 365 فعلياً بقاعدة البيانات بعد تأكيد حقيقي عبر الواجهة لكل واحد (لا seed)؛ حساب بمعدل رابح (`daysConsumed=10` من 365، أرقام محسوبة يدوياً: معدل يومي +5,000، توقع نهائي 1,925,000، 355 يوم متبقي) عرض التوقع بالضبط بدون أي حكم قاطع وبحكم توقعي "على المسار للنجاح"؛ حساب بمعدل خاسر (نفس المدة، معدل يومي -5,000، توقع نهائي -1,525,000) عرض "على المسار للخسارة" بالأرقام الصحيحة بالضبط؛ `daysConsumed=0` أعطى رسالة "لا بيانات كافية" فعلياً بدون أي خطأ حسابي أو تحطّم صفحة؛ فرض `daysConsumed=400` (أكبر من 365) مباشرة بقاعدة البيانات أعطى **نفس الشاشة القديمة بالضبط** (حكم قاطع "نجحت"، بدون أي أثر لعنوان التوقع) — تأكيد إن هالسلوك لم يتغيّر؛ رسالة ضغط المستثمر بالـHUD (`isOverdue`) ضلّت تعمل صح بالعتبة الجديدة 365. **ملاحظة أمانة عن جودة التشخيص:** فحصان أوليان أعطيا فشلاً زائفاً بسبب خطأ بصياغة نص الفحص نفسه (ترتيب "+"/"AED" بالقالب) وحرف تحكّم اتجاه غير مرئي (`U+200E`، Left-to-Right Mark) بيضيفه `toLocaleString("ar")` قبل الأرقام السالبة تلقائياً (يظهر بصرياً طبيعي تماماً للمستخدم، بس يكسر مطابقة نص حرفية بسيطة) — تأكدت بفحص مباشر لرموز الأحرف (`char codes`) بالمتصفح الحقيقي، صحّحت الفحص (إزالة حروف التحكّم قبل المطابقة)، ونجح 100% — الكود نفسه كان سليماً طول الوقت، الخلل كان بمنهجية اختباري أنا فقط. بيانات الاختبار انحذفت بالكامل من فرع `development`.

---

## ملاحظات معلّقة
_(أفكار أو تعديلات مقترحة أثناء البناء، بانتظار المراجعة بعد الديمو)_
- نماذج `AuthView` (تسجيل دخول/حساب) حالياً بالإنجليزي بالكامل (Name/Email/Password/Create an account...) — بدون أي تعريب. يستاهل مراجعة لاحقاً (تحتاج ضبط `localization` prop على `NeonAuthUIProvider`).
- في تحذير Hydration Mismatch بسيط بالكونسول (`style={{color-scheme:"light"}}`) جاي من مكتبة `@neondatabase/auth-ui`/`next-themes` — موجود من وقت ما ضفنا المصادقة، مو من كودنا، وغير مؤثر على الوظائف. يستاهل متابعة لو تكرر أو أثّر لاحقاً.

---
